# 量化引擎 v10.0 实盘接入设计文档

> 文档用途：供后续券商工程师评审与实现。当前交付严格只有模拟盘，不包含 QMT、券商 SDK、账号登录、实盘委托或撤单代码。
> 本文描述系统边界和验收要求，不构成投资建议，也不是可直接执行的实盘程序。

## 1. 目标与边界

未来若接入实盘，必须保留当前“信号 → 拟交易清单 → 风控门禁 → 人工确认/自动授权 → 执行 → 对账”的分层流程。
禁止把 `server/paper_engine.py` 的 SQLite 模拟撮合直接替换为券商下单调用，也禁止让网页前端直接持有券商凭证。

当前版本已经完成：

- 多虚拟账户、复合信号、拟交易清单、T+1、费用、成交、持仓和财富曲线。
- 安全确认与模拟自动两种模式；两者都只写本机 SQLite。
- 新闻、因子、趋势三类输入及组合级仓位限制。

未来券商工程师负责：

- 券商授权的行情与交易 SDK 适配。
- 账号登录、会话保活、委托、撤单、成交回报和资产查询。
- 券商侧规则校验、真实账户对账、异常恢复和生产部署。

## 2. 推荐隔离架构

```text
浏览器前端
  │ 只提交规则、观察池和人工确认
  ▼
量化引擎 API（现有 FastAPI）
  │ 产生不可变的拟交易清单，不接触券商凭证
  ▼
实盘风控网关（未来独立服务）
  │ 二次校验、幂等、防重复、授权状态、交易时段
  ▼
券商适配器（未来由券商工程师实现）
  │ 仅使用券商正式 SDK / 授权接口
  ▼
券商柜台

券商成交与资产回报 ──► 对账服务 ──► 独立实盘账本 ──► 只读回传前端
```

实盘风控网关和券商适配器应运行在独立进程，使用独立数据库。模拟表与实盘表不能共用，模拟账户 ID 也不能被解释为真实资金账号。

## 3. 数据契约

量化引擎只向未来风控网关交付“意图”，不得交付未经校验的券商原始指令。每条意图至少包含：

| 字段 | 要求 |
|---|---|
| `intent_id` | 全局唯一、不可复用，作为幂等键 |
| `created_at` / `expires_at` | 生成时间与过期时间；过期后禁止执行 |
| `account_alias` | 引用受控账户别名，不出现账号或密码 |
| `symbol` / `market` | 标准化证券代码与市场 |
| `side` | 只允许白名单动作 |
| `quantity` | 已按最小交易单位取整的数量 |
| `price_policy` | 限价/受保护市价等经券商确认的策略 |
| `max_slippage` | 最大可接受偏离，超限拒绝 |
| `signal_snapshot` | 新闻、因子、趋势分及规则版本快照 |
| `approval` | 人工确认人、时间、设备或自动授权策略版本 |

风控网关返回统一状态：`received`、`risk_rejected`、`submitted`、`partially_filled`、`filled`、`cancelled`、`broker_rejected`、`unknown`。
任何超时不得直接视为失败并重下；必须先按 `intent_id` 和券商委托号查询，避免重复成交。

## 4. 强制风控门禁

每笔意图在进入券商适配器前，至少重新检查：

1. 实盘总开关、账户授权和当日人工解锁是否有效。
2. 交易日期、交易时段、证券停牌、涨跌停和最小交易单位。
3. T+1 可卖数量，以券商返回为最终依据；本地估算只能提前拦截。
4. 可用资金、冻结资金、可用持仓和未完成委托。
5. 最大持仓 3 只、单标的 40%、总仓 95% 等组合限制。
6. 单笔金额、单日成交额、单日亏损、连续拒单和最大回撤熔断。
7. 同标的冷却期、重复意图和自成交风险。
8. 价格偏离、盘口流动性和最大滑点；行情过期时直接拒绝。

任何关键数据缺失、券商查询超时、对账不一致或系统时钟异常，都应“拒绝新开仓”，不能自动放行。

## 5. 安全/自动双模式

**安全确认模式**是首次上线的唯一允许模式：拟交易清单经过风控预检后，由用户逐笔确认；确认动作应二次验证并记录审计信息。

**自动模式**必须在安全模式稳定运行、完成灰度验收后才可由券商工程师启用，并满足：

- 账户、标的、时段、最大金额和策略版本白名单。
- 每日有时效的人工解锁，默认重启后回到锁定状态。
- 独立紧急停止开关；停止后仅允许撤单和查询，不允许新委托。
- 自动策略异常时降级到安全确认，不能静默继续。

## 6. 对账与恢复

真实资产以券商为准。系统至少在开盘前、成交回报后、收盘后执行三类对账：

- 委托对账：本地意图、券商委托号、状态、拒绝原因一一对应。
- 成交对账：部分成交、多次成交、费用和成交时间完整落账。
- 资产对账：现金、冻结资金、持仓数量、可卖数量和市值与券商一致。

若回报流断开，应先停止新委托，再从券商查询接口补齐状态。出现 `unknown` 状态时禁止用同一交易意图重试，直到人工或查询结果消除不确定性。

## 7. 安全与审计

- 券商凭证只能放在操作系统凭证库或受控密钥服务中，不能写入 `.env`、SQLite、日志或前端。
- 风控网关仅监听本机或受控内网，使用双向认证；禁止暴露到公网。
- 日志应记录规则版本、输入快照、风控结果、确认人和券商回报，但必须脱敏。
- 关键表采用追加式审计，不允许通过普通界面删除或改写成交历史。
- 系统启动时默认实盘锁定；模拟盘永远可独立运行，实盘故障不能污染模拟账本。

## 8. 券商工程师验收清单

- [ ] 未安装券商适配器时，现有模拟盘功能完整可用。
- [ ] 前端和量化引擎进程中均找不到券商密码、令牌或私钥。
- [ ] 同一个 `intent_id` 重放 100 次只产生一个有效委托。
- [ ] 网络超时、回报乱序、部分成交和重启恢复均不会重复下单。
- [ ] 当日买入、停牌、资金不足、行情过期、超仓和超滑点全部被拦截。
- [ ] 紧急停止后不再产生新委托，且查询和撤单仍可用。
- [ ] 券商资产与本地账本不一致时自动锁定并给出可追溯差异。
- [ ] 安全确认模式连续完成仿真、券商测试环境和小范围灰度验收。
- [ ] 每次发布都有回滚方案，回滚不会丢失未完成委托状态。

## 9. 实施顺序

1. 先冻结并版本化拟交易数据契约。
2. 建立独立风控网关和只读券商查询适配器，只做资产/委托查询。
3. 完成对账、幂等、审计和故障注入测试。
4. 在券商测试环境接入委托与撤单，仍保持安全确认模式。
5. 完成券商、安全与业务三方验收后再讨论受限自动模式。

当前 v10 已完成第 1 步的数据契约雏形：`live_snapshots` 保存脱敏只读快照，`order_intents` 保存带稳定幂等编号的不可执行订单意图，导出包固定标记 `execution_enabled=false`。
程序仍未进入第 2 步：没有券商查询适配器、凭证、风控网关或任何交易执行能力。后续实盘实施必须由具备券商接口权限和生产责任的工程师独立完成。
