# 量化引擎优化建议报告

生成日期：2026-08-03
依据：4 个抖音视频的页面信息 + 对现有引擎源码的逐项审查

---

## 〇、先说清楚信息来源的边界

抖音对未登录访问有登录墙（"登录后免费畅享高清视频"），**视频的画面与语音我无法获取**。
我实际拿到的是：标题、时长、作者、发布时间、以及**完整评论区文本**。

因此本报告中：

- 标 **〔视频页〕** 的内容 = 从页面上抓到的真实文本
- 标 **〔审查〕** 的内容 = 我对你现有代码的实际检查结论
- 其余是我基于两者给出的判断

如果你能把视频的文字稿（抖音有自动字幕，评论区也有人让豆包转写）发我，我可以据此再修一版。

---

## 一、四个视频的可提取信号

### 1. 考拉数理｜0:32｜小白如何用 deepseek 实现量化交易

娱乐向短视频（"万物皆可手搓"系列第 104 集）。**真正有价值的是评论区的集体反驳**〔视频页〕：

> "兄弟们，程序好写，数据接口很贵很贵，一般人真的用不起"（614 赞）
> "真的量化是消息比你获取得早，挂单排队在你前面，跑得先跑，买比你先买，得给交易所交钱拿专线"（126 赞）
> "他们的高频设备全是最好的毫秒级交易时间……普通散户玩不过的"（132 赞）

**指向的结论**：散户和机构的差距不在"会不会写策略"，而在**数据质量、延迟、通道**。
这恰好划定了我们这类工具的合理定位——只能做**低频（日线级）**，任何依赖速度的东西都不必碰。

### 2. 数字生命雷恩加尔｜0:56｜13K Star 开源量化引擎

评论区两条关键提问〔视频页〕：

> "实时行情接什么"
> "回测和实盘差距还是挺大的"

**指向的结论**：开源引擎解决的是"接口打通"，但**回测→实盘的落差**是所有人共同的痛点。
这是本报告第三节的核心。

### 3. 布韦｜2:39｜A股量化系统完工总结

标题本身就是一份架构清单〔视频页〕：**六大策略 + QMT + 回测 + 模拟盘 + 实盘 + 首周模拟盘 +1.89%**。

- **QMT** = 券商量化交易终端（迅投），散户做 A 股程序化交易的标准路径，评论提到"QMT 10 可开通"（10 万资产门槛）
- 关键是这条链路：**回测 → 模拟盘 → 实盘**，中间的"模拟盘"是我们完全缺失的一环
- 评论"就缺策略了，其他都搭建好了，现在在研究多因子" —— 说明大家卡的都是同一处：**基建容易，有效信号难**

### 4. 做量化的西蒙｜14:14｜普通人做 AI 量化，第一步就错了

四个视频里唯一的长视频，评论区技术含量最高〔视频页〕：

> "一个因子挖掘，算一个 rank IC，基本上就可以劝退 99% 的人了，什么回测、模型，根本都不需要上"（**作者点赞**）
> "新闻因子跑三十遍随机参数误差都很大——这正是过拟合的典型症状：参数一抖结果就乱，说明信号没穿过噪声。更稳的做法是滚动样本外（walk-forward）验证，或把参数锁进固定因子框架只调权重"
> "量化只是帮我节约时间……与我个人观点相悖的我会仔细看，然后用另一个程序去用相反方向验证"

**指向的结论**——这是全部四个视频里对我们最有用的一条：

1. **先验证信号有没有预测力（IC / Rank IC），再谈回测**
2. **过拟合的判据是参数敏感性**：参数微调结果就崩，说明是噪声
3. **样本外 / walk-forward 验证**才是防过拟合的正解

---

## 二、我们已经做对的部分〔审查〕

对照上述标准逐条检查了现有代码，以下几点是站得住的：

| 项目 | 现状 | 评价 |
|---|---|---|
| 未来函数 | 信号在第 i 根收盘确认，成交价取第 i+1 根**开盘** | ✅ 正确，无未来数据泄漏 |
| T+1 约束 | 第 i+1 根开盘买入 → 最早第 i+2 根开盘卖出，隔夜持有 | ✅ 天然满足 A 股 T+1 |
| 交易成本 | 手续费+滑点按单边千分比**双边**扣除 | ✅ 方向正确 |
| 仓位 | 半凯利，且胜率/赔率取**实测**而非拍脑袋 | ✅ 比绝大多数教程严谨 |
| 期望收益一致性 | EV 与凯利同口径，必然同号 | ✅ 上一轮已修正 |
| 免责声明 | 界面、报告、日志三处 | ✅ |

---

## 三、发现的问题（按严重程度排序）〔审查〕

### ✅ P0-1：板块指数根本不能交易，但引擎在回测它 —— 已于 2026-08-07 修复

现在选「板块 → 半导体 BK1036」可以跑出完整的回测报告和净值曲线。
**但东财板块指数是计算出来的指数，没有任何渠道能买卖它。**

这不是精度问题，是**逻辑性质的失真**：报告里那条净值曲线对应的交易在现实中不存在。
小白极易把"半导体板块回测 +19.8%"理解成"买半导体能赚 19.8%"。

**建议**：板块与指数标的在回测时打上「不可直接交易」标记，报告顶部显式提示
"本标的为指数，不可直接买卖；如需交易请选择跟踪它的 ETF"，并给出对应 ETF 的跳转。

**已实现**：载入板块/指数时，图表副标题出现红色「不可直接交易」标签，
终端日志给出警告，回测报告顶部显示红框提示"这条净值曲线对应的交易在现实中并不存在"。
判定依据取自标的库分类（股票/场内基金/场外基金可交易，板块/指数不可）。

### ✅ P0-2：场外基金的赎回费被严重低估 —— 已于 2026-08-07 修复

这是我上一轮新增场外基金时留下的实际缺陷。

现在场外基金和场内用同一个「手续费+滑点 ‰」（默认 1.5‰）。但真实的场外基金费率是：

| 持有期 | 典型赎回费 |
|---|---|
| < 7 天 | **1.50%**（强制，监管规定） |
| 7–30 天 | 0.75% |
| 30 天–1 年 | 0.50% |
| > 1 年 | 0.25% 或免收 |

外加申购费 0.1%–1.5%（很多平台打一折）。

我们的策略最长持有 60 个交易日（约 3 个月），**绝大多数交易会落在 0.5% 赎回费档**，
是当前默认设置的 **3 倍以上**；若触发短线止损在 7 天内卖出，则是 **10 倍**。

**后果**：场外基金的回测收益被系统性高估。上一轮那些 +30.4%、+19.8% 的满仓口径结果，
扣掉真实赎回费后可能所剩无几，甚至转负。

**建议**：场外基金改用**按持有天数分档**的赎回费模型，界面单独给一组费率输入。

**已实现**：赎回费按**日历天数**（从开仓日到平仓日的实际天数，不是交易日）自动分档
1.5% / 0.75% / 0.5% / 0.25%；费率输入框在选中场外基金时自动切换为「申购费 %」（默认 0.15%）；
交易明细表新增「费率」列，可逐笔核对。

**修正前后实测对比**（易方达消费行业 110022，同一批策略，满仓口径）：

| 策略 | 修正前 | 修正后 | 差异 |
|---|---|---|---|
| 平台整理突破 | +15.3% | **−0.9%** | −16.2pp |
| 唐奇安通道突破 | +19.8% | +5.1% | −14.7pp |
| 月度动量 | +16.6% | +5.1% | −11.5pp |
| 均线多头排列 | −8.3% | −20.7% | −12.4pp |
| 双均线金叉 | +30.4% | +24.4% | −6.0pp |

**8 个策略平均被高估约 9 个百分点**，其中「平台整理突破」由盈转亏。
这印证了原判断：此前场外基金的回测收益是系统性虚高的。

### 🟠 P1-1：没有样本外检验，且产品设计在鼓励过拟合

这是视频 4 评论区直指的问题，也是当前引擎最大的统计缺陷。

现状：一次性在全部约 4 年数据上回测，没有训练/测试切分。
更糟的是交互设计——**「生成 10 个策略」然后让用户挑收益最高的那个**，
这是教科书级的**选择偏差 / 数据窥探**：在 21 个策略里挑最好的，
即使全都是随机噪声，也必然有一个看起来很漂亮。

**建议（按投入产出排序）**：

1. **样本内/样本外切分**：前 70% 训练、后 30% 检验，报告并列显示两段绩效。
   两段差距大 = 过拟合。这是最便宜、最有效的一招。
2. **随机基准对照**：用相同交易次数和持仓天数生成 200 组随机进场，
   看真实策略的收益落在随机分布的第几百分位。**低于 95 分位就是噪声。**
   对小白来说这一个功能的教育价值超过所有绩效指标。
3. **参数敏感性**：止损/目标各上下浮动 ±20%，看收益是否稳定。
   "参数一抖结果就乱"就是过拟合的判据。
4. **多重检验提示**：生成 N 个策略后，在报告里注明
   "你在 N 个策略中挑选，最优者的表现天然被高估"。

### 🟠 P1-2：缺少因子有效性检验（IC / Rank IC）

视频 4 评论区被作者点赞的那条说得很直白：先算 rank IC，不合格就不用回测了。

现状：我们直接从"策略规则"跳到"绩效报告"，中间没有任何**信号质量**的度量。

**建议**：为每个策略增加一个 **IC 面板**：
计算信号强度与未来 N 日收益的 **Spearman 秩相关**（N = 5/10/20），
给出 IC 均值、IC 标准差、**ICIR = IC均值/IC标准差**、IC 胜率。

判读标准（写进界面）：
- |IC| < 0.02 → 基本无预测力
- 0.02–0.05 → 弱信号
- \> 0.05 → 在 A 股日线级别已算不错
- ICIR > 0.3 → 信号相对稳定

这样在看到漂亮净值曲线**之前**，先知道这个信号到底有没有信息量。

### 🟡 P2-1：没有涨跌停约束

A 股一字涨停时买不进、一字跌停时卖不出。
突破类策略（唐奇安、平台突破、跳空缺口）最容易在涨停当天触发信号，
而次日开盘往往高开甚至一字板——**回测按开盘价成交，现实中根本买不到**。

**建议**：次日开盘价若较前收涨幅 ≥9.8%（创业板/科创板 ≥19.6%）则视为无法成交，跳过该次交易。

### 🟡 P2-2：缺少"模拟盘"这一环

视频 3 的链路是 **回测 → 模拟盘 → 实盘**，我们只有第一步。

回测是"在已知答案的历史上考试"，无论怎么防过拟合都有偏差；
**模拟盘是唯一诚实的检验**：策略选定后，从今天起每天记录信号与假设成交，跑三个月看真实表现。

**建议**：新增「模拟盘」标签页，把选中的策略挂上去，每天用实时快照推进一步，
持久化到 localStorage，累计真实的前瞻绩效。**这是把玩具变成工具的关键一步。**

### 🟡 P2-3：「AI 生成策略」名不副实

现在这个按钮是"根据均线斜率判断趋势/震荡，然后从模板库里随机抽一个"。
这是**规则匹配**，不是 AI，也不产生新策略。视频 4 的标题恰好是"第一步就错了"。

**建议**：要么改名为「按市况匹配策略」（诚实），
要么做成真正有价值的形态——让它**批量跑 IC + 样本外 + 随机基准，然后给出淘汰理由**。
AI 在量化里的真实价值是**批量证伪**，不是"生成能赚钱的策略"。

---

## 四、优先级建议

| 优先级 | 事项 | 工作量 | 价值 |
|---|---|---|---|
| ~~P0~~ ✅ | ~~板块/指数标注"不可交易"~~ 已完成 | 小 | 消除性质性误导 |
| ~~P0~~ ✅ | ~~场外基金分档赎回费~~ 已完成 | 小 | 修正系统性高估 |
| P1 | 样本内/样本外切分 | 中 | 过拟合的第一道防线 |
| P1 | 随机策略基准对照 | 中 | 对小白教育价值最高 |
| P1 | IC / ICIR 面板 | 中 | 回测之前先看信号质量 |
| P2 | 涨跌停不可成交约束 | 小 | 突破类策略的真实性 |
| P2 | 模拟盘（前瞻验证） | 大 | 从玩具走向工具 |
| P2 | 参数敏感性 | 中 | 过拟合的直观判据 |
| P3 | 「AI 生成策略」改名或重做 | 小 | 诚实性 |

**如果只做三件事**：分档赎回费、样本外切分、随机基准对照。

---

## 四·五、待办汇总表（累计，逐条修整用）

来源标记：**审**=代码审查｜**评**=视频评论区｜**图**=视频1截图流程｜**Q**=QuantDinger 对照

| # | 事项 | 来源 | 工作量 | 优先级 | 状态 |
|---|---|---|---|---|---|
| 1 | 板块/指数标注「不可直接交易」 | 审 | 小 | P0 | ✅ 已完成 |
| 2 | 场外基金分档赎回费 | 审 | 小 | P0 | ✅ 已完成 |
| 3 | 样本内 / 样本外切分（前70%训练，后30%检验） | 评·Q | 中 | **P1** | ✅ 已完成 |
| 4 | 随机策略基准对照（200组随机进场，看百分位） | 审 | 中 | **P1** | ✅ 已完成 |
| 5 | IC / ICIR 因子有效性面板 | 评 | 中 | **P1** | ✅ 已完成 |
| 6 | 参数敏感性（止损/目标 ±40% 网格 + 稳健区域识别） | 评·图·L | 中 | P1 | ✅ 已完成 |
| 7 | 特征分层（收益率/波动率/量能/趋势），作为 #5 的前置 | 图 | 中 | P1 | ✅ 已完成 |
| 8 | 多标的批量扫描（同一策略跑 N 个标的看稳健性） | Q | 中 | P1 | ✅ 已完成 |
| 9 | 涨跌停无法成交约束 | 审 | 小 | P2 | ✅ 已完成 |
| 10 | 最小交易单位 100 股 + 初始资金概念 | 图 | 小 | P2 | ✅ 已完成 |
| 11 | 模拟盘（前瞻验证，每日推进） | 图·Q | 大 | P2 | ☐ |
| 12 | 行情本地缓存（IndexedDB），离线复用+提速 | Q | 中 | P2 | ☐ |
| 13 | ~~默认费率改 0.5‰ 并注明构成~~ 已被 #10/#20 取代：费率已拆为 佣金2.5‱(最低5元)+滑点5‱+印花税5‱，报告副标题常驻标注 | 图 | 很小 | — | ✅ 已完成 |
| 14 | 策略规则可导出为可读文本/代码 | Q | 中 | P3 | ✅ 已完成 |
| 15 | 「AI 生成策略」改名或重做为批量证伪 | 评 | 小 | P3 | ✅ 已完成 |
| 16 | **改用后复权（fqt=2）回测**，前复权数据会追溯变化导致结果不可复现 | L | 小 | **P1** | ✅ 已完成 |
| 17 | 单笔金额占当日成交额过高时警告（滑点不该是固定值） | L | 小 | P2 | ✅ 已完成 |
| 18 | #6 升级：参数网格扫描 + 稳健区域识别（孤立尖峰=过拟合） | L | 中 | P2 | ☐ |
| 19 | #8 多标的扫描须标注幸存者偏差（标的库只含在市标的） | L | 很小 | P2 | ✅ 已完成 |

| 20 | **最低佣金 5 元**（小资金下真实费率可翻数倍） | 东 | 很小 | **P1** | ✅ 已完成 |
| 21 | 组合层：最大持仓数 / 单票上限 / 总仓上限 / 冷却期 / Top-N 进出 | 东·用 | 中 | P2 | ✅ 已完成 |
| 22 | ATR 动态止损 + 跟踪止盈（盈利3%激活、回撤5%止盈） | 东·用 | 小 | P2 | ✅ 已完成 |
| 23 | 双数据源 + SQLite 本地缓存 | 东·用 | 大※ | P3 | ☐ |
| 24 | 模拟盘引擎（虚拟券商 + 调度器 + SSE 流式） | 东·用 | 大※ | P3 | ☐ |
| 25 | QMT 桥接（Python 3.6.8 ↔ 主系统，xtdata 行情） | 东·用 | 大※ | P3 | ☐ |
| 26 | 安全 / 自动双模式切换（信号生成 → 人工确认 → 执行） | 东·用 | 中※ | P3 | ☐ |
| 27 | 实盘下单（xttrader → 交易所） | 东·用 | 大※ | P3 | ⛔ 见下 |

| 28 | **仓位改用策略自身样本内胜率/赔率**（当前用"随便买"的统计，系统性偏小） | 实验 | 中 | **P1** | ✅ 已完成 |
| 29 | 敏感性扫描扩到因子策略的三个参数（分位窗口/持有期/分位阈值） | 实验 | 中 | P2 | ✅ 已完成 |
| 30 | **风险调整口径**（夏普/卡玛/同等回撤下收益）—— 现有五关只看超额，对择时轻仓策略不公平 | 实验 | 中 | **P1** | ✅ 已完成 |
| 31 | **组合回测也要过关卡**：调仓周期/持仓数/上限的敏感性 + 样本内外 + 随机选股基准 | 实验 | 中 | **P1** | ✅ 已完成 |
| 32 | **资产配置模块**：宽基组合分散 + 定投模拟 + 有效前沿（不依赖预测能力的一条路） | 用 | 中 | **P1** | ✅ 已完成 |
| 33 | **美股标的库**（13718 只）+ 美股成交规则（1股起买/无印花税/无涨跌停/美元计价/美东时段） | 用 | 中 | **P1** | ✅ 已完成 |
| 34 | **策略面板**：参数折叠 + 紧凑列表 + 排序筛选 | 用 | 中 | P2 | ✅ 已完成 |
| 35 | **我的持仓**（SQLite 持久化 + 市值/当日盈亏/累计盈亏） | 用 | 大 | **P1** | ✅ 已完成 |
| 36 | **市场每日情绪**（新闻抓取 + 大模型情绪打分 + 结合持仓的每日参考建议） | 用 | 大 | **P1** | ✅ 已完成 |
| 12 | SQLite 本地缓存 —— 由 #35 的后端一并落地 | 东·用 | — | — | ✅ 随 #35 完成 |

来源补充：**L** = QuantConnect LEAN 对照｜**实验** = 反转假设检验过程中发现｜**东** = 东方A股量化交易系统（视频3）｜**用** = 用户指定
※ = 需要架构改造，见下节

---

## 四·八、2026-08-07 第二批改动记录（#16 / #10 / #20）

### 改了什么

1. **复权方式可选，默认后复权**（图表右上角）。前复权/不复权可切换，切换即自动重新取数并在日志说明取舍。
2. **回测引擎从"按百分比连续建仓"改为"按元计价"**：
   - 初始资金（默认 10 万）成为一等参数
   - 场内按 **100 股整手**成交，买不起一手则跳过该次信号并在策略卡上计数
   - 费用拆开：**佣金 ‱（每笔最低 5 元）+ 滑点 ‱（双边）+ 印花税 5‱（卖出单边）**
   - 场外基金：申购费 % + 按持有日历天数分档的赎回费
3. 交易明细新增**股数、费用(元)、权益(元)**；单笔收益改为"相对本笔投入"而非"相对总权益"。

### 过程中发现并修正的一个自造缺陷

先只把 `fqt` 改成 2（后复权）时，贵州茅台显示 **8187 元/股**——后复权累计了它上市 25 年的全部分红，
绝对价位是真实市价（约 1312）的 **6.2 倍**。于是"一手要 81.9 万"，10 万本金全部信号被跳过。

**用后复权价算股数是错的。** 正确做法是双序列：

| 用途 | 用哪条序列 |
|---|---|
| 指标、收益率、止损止盈判定 | 后复权（分红不会被误算成亏损） |
| 股数、成交金额、佣金印花税 | **不复权（真实市价）** |
| 顶栏报价显示 | 不复权 |
| 图表绘制 | 后复权（图注已标明） |

实现上每次取数发两个请求（fqt=所选 + fqt=0），按日期对齐；第二个请求失败则退回主序列并保持可用。
持仓价值 = 开仓金额 × 复权倍数，这样分红既不被算成亏损、金额又落在真实尺度上。

### 实测效果

**茅台**：一手真实价 13.1 万 > 10 万本金 → 全部信号跳过、0 笔成交。
这是正确的现实——改之前引擎会假装买了 0.76 手并给出收益率。

**沪深300ETF，同一策略、只改初始资金**：

| 初始资金 | 单笔金额 | 每笔费用 | 往返费率 | 累计收益 |
|---|---|---|---|---|
| 2 万 | 1206 元 | 11 元 | **0.91%** | **−0.52%** |
| 100 万 | 62400 元 | 62 元 | **0.099%** | **+0.80%** |

**小资金的交易成本是大资金的 9.2 倍**，同一个策略一个亏一个赚。
全部来自「每笔最低佣金 5 元」——改之前两者结果完全相同。

---

## 四·八·二、2026-08-07 第三批改动记录（#4 随机进场基准）

### 做法

打开任意回测报告时自动运行：保持**相同的交易次数、相同的止损/止盈/最长持仓、相同的仓位与费用**，
只把**进场时点换成随机**，跑 200 组，看真实策略的收益落在这 200 组的第几百分位。

这样能干净地隔离出一个问题：**进场信号本身有没有价值？**
如果一条净值曲线在随机进场里毫不出奇，那它的形状来自止损止盈规则和标的自身走势，与信号无关。

判定档位：≥95% 显著｜80–95% 证据偏弱｜<80% 与随机无异。
交易 <5 笔时不做对照，直接说明样本不足（而不是把面板藏掉）。

### 首次全量实测（半导体ETF国联安，10 个做多策略）

| 判定 | 数量 |
|---|---|
| ✔ 显著（≥95%） | **1** |
| ～ 偏弱（80–95%） | 2 |
| ✖ 与随机无异 | **7** |

典型例子：

- 「20日线回踩企稳」回测 **+3.87%**，看着是赚的 —— 但随机进场中位数是 **+4.77%**，
  百分位仅 42%。**随便乱买比按信号买还好一点。**
- 「三连阳启动」46 笔交易，回测 −0.61%，随机中位 +7.70%，百分位 **9%**。
- 唯一通过的是「平台整理突破」，97.5%。

**10 个策略里 7 个的漂亮曲线其实是止损止盈规则 + 标的本身走势的产物。**

### 过程中修掉的两个问题

1. **临界策略结论会翻转**：随机分布每次重跑不同，「双均线金叉」一次 95.5%（通过）、
   一次 93.5%（不通过）。已改为用「策略名 + 标的代码 + 交易笔数」派生**固定种子**，
   同一组合任何时候重跑分布完全一致（已连开三次验证）。这与 #16 的可复现性原则一致。
2. **样本不足时面板静默消失**：交易 3 笔的策略直接把整块隐藏，用户不知道为什么。
   改为显示说明并清空旧数值（原先还会残留上一份报告的数字）。

### 性能

200 组完整回测（含整手、最低佣金、印花税）耗时约 **65ms**，打开报告无感知延迟。

---

## 四·八·三、2026-08-07 第四批改动记录（#3 样本内/样本外）

### 做法

回测报告里自动切分：前 70% 当作"你能看到的历史"，后 30% 当作"未来"，
两段各自从初始资金重新起算，并列显示。回测引擎新增可选区间参数以复用同一套撮合逻辑。

### 中途发现并修正的判定盲点

第一版只对比"策略的样本内 vs 样本外"，结果所有策略都显示**样本内亏、样本外赚**——
与典型过拟合模式相反，看上去像是策略变好了。

真相是：该标的在后 30% 恰好是大涨行情。**策略赚的是行情的钱，不是信号的钱。**

已加入**同期买入并持有基准**，判定改为看**超额（策略年化 − 买入持有年化）**是否延续。
加基准前后，同一批策略的结论完全相反：

| 策略 | 策略年化（内→外） | 买入持有（内→外） | 超额（内→外） |
|---|---|---|---|
| 三连阳启动 | −2.83% → **+5.93%** | +6.88% → **+82.98%** | −9.71% → **−77.04%** |
| 唐奇安通道突破 | −1.17% → **+9.44%** | +6.88% → +82.98% | −8.04% → **−73.53%** |
| 布林下轨反弹 | −1.38% → +0.27% | +6.88% → +82.98% | −8.25% → −82.70% |

半导体ETF 在样本外区间躺着不动的年化是 **+82.98%**，最好的策略只做到 +9.44%。
**所有策略都在大幅跑输买入持有 70–83 个百分点。**

> 这条教训值得单独记：**任何策略的收益都必须减去同期买入持有，剩下的才是它的本事。**
> 只看绝对收益，牛市里随便什么策略都"赚钱"。

### 判定档位

1. 样本外 <5 笔 → 无法判断
2. 样本内超额 ≤0 → 连自己熟悉的历史都跑不赢买入持有，不如直接持有
3. 样本外超额 ≤0 → 典型过拟合，样本外的正收益来自行情本身
4. 样本外超额 ≥ 样本内的 50% → 超额有延续性
5. 其余 → 衰减明显

---

## 四·八·四、2026-08-07 第五批改动记录（#6 参数敏感性）

### 做法

止损与目标各取当前值的 ×0.6 / ×0.8 / ×1.0 / ×1.2 / ×1.4，组成 5×5 = 25 格，
每格跑一次完整回测，画成热力图（红=正超额，绿=负超额），当前设置用金框标出。

判断依据：真实的规律在参数平面上应该是**一整片**有效区域；
如果只有当前这一格好、旁边全差，那是**孤立尖峰**，说明这组参数是被历史"挑"出来的。

### 中途修正的两个设计问题

**① 小仓位淹没了区分度。**
第一版按策略的建议仓位扫描，结果 25 格数值几乎相同（−5.97 到 −5.99）。
原因：仓位只有 6.3% 时策略几乎不参与市场，超额被"负的买入持有年化"这个常数主导。
改为**统一按满仓扫描**，与买入持有同口径，数值随即展开到 +2.22% ~ −8.61%。

**② "参数无关"被误判成"稳健"。**
不少策略 25 格完全相同（极差 0.00%）——因为它们的离场信号（如"跌破10日均线"）
总是先于止损/止盈触发，那两个参数**根本没参与决策**。
这种情况下网格再平坦也不代表稳健，只代表没测到东西。
已加入极差检测：极差 <0.5% 时单独提示"止损与目标几乎不影响结果，本次扫描没有真正检验到稳健性"，
并指出实际的离场规则是哪一条。

### 实测（沪深300ETF，10 个做多策略）

25 格全部为正超额的：**1 个**（三连阳启动，20/25 格为正，判定稳健）。
其余多为 0/25，且不少属于"止损止盈从未生效"那一类。

---

## 四·八·五、2026-08-07 第六批改动记录（#8 跨标的扫描）

### 做法

报告里新增「跨标的扫描」，把当前策略拿到**当前分类的常用标的篮子**（最多 16 个）上各跑一遍，
统计有多少个能跑出正超额，并标出你正在看的这个标的排第几。

因为要联网逐个取数，设计成**按钮触发**而非自动运行；3 路并发 + 140ms 间隔避免限流；
取过的数据在会话内缓存，重复扫描瞬时完成。实测 16 个标的约 2 秒。

判定：≥70% 标的为正且中位为正 → 最强证据；50–70% → 接近抛硬币；<50% → 不具普适性。
若你看的标的恰好排第 1，会单独点出这是典型的**挑标的（selection bias）**。

### 实测揭示的一个模式

「布林下轨反弹」在 16 个 ETF 上只有 **3/16** 正超额，中位 −5.78%，
而一直在分析的半导体ETF **排名 16/16（最差）**。

更值得注意的是数据里的规律：

| 标的 | 买入持有 | 策略 | 超额 |
|---|---|---|---|
| 消费ETF | **−11.01%** | −3.99% | **+7.02%** |
| 酒ETF | −10.22% | −4.22% | +6.00% |
| 科创50ETF | **+16.62%** | +1.71% | −14.91% |
| 半导体ETF | **+27.01%** | −6.66% | **−33.68%** |

**在下跌标的上跑赢、在上涨标的上大幅跑输** —— 这个"抄底"策略本质上不是在创造超额，
而是在**降低市场暴露**：熊市少亏、牛市少赚。这不是 alpha，是把 beta 调小了。

单看一个标的永远发现不了这一点。

### 已知局限

篮子取自"今天仍在市"的常用标的，**已退市品种不在其中**（幸服者偏差，对应待办 #19）。
界面已在结论下方常驻标注。

---

## 四·八·六、2026-08-07 第七批改动记录（#7 特征层 + #5 因子 IC）

### 做法

策略引擎新增「◎ 因子检验」按钮，打开独立窗口。把散落在各策略里的指标抽成
**11 个因子 / 4 个族**（收益率、波动率、量能、趋势），对每个因子计算：

- **IC(5/10/20日)**：因子在第 t 天的取值，与该标的未来 N 天收益率的 **Spearman 秩相关**
- **ICIR**：按 120 根分块算 IC(10)，取 均值/标准差，衡量这个 IC 在时间上稳不稳
- **分段为正**：多少个分块的 IC 同号

判读标准（A股日线级别）：|IC|<0.02 无预测力，0.02–0.05 偏弱，>0.05 较强；|ICIR|>0.3 算稳定。

**口径已在界面注明**：这是**时序 IC**（同一标的、跨时间），用于判断择时信号；
机构常说的截面 Rank IC 是"同一天、跨多只标的"排序，两者含义不同，不可混为一谈。

### 中途修正的判读缺陷

第一版只看 |IC| 大小，结果把 IC=−0.045 但 **ICIR=−0.88、7 段中仅 1 段为正**的「5日动量」
判成"偏弱"，反而把 IC=+0.107 但 **ICIR 只有 0.25、7 段中 4 段为正**的「60日年化波动」判成"较强"。

**稳定性比幅度更重要**：一个 IC 不大但在各分段都同号的因子，比 IC 大却飘忽的可信得多。
判读已改为综合 |IC| 与 |ICIR|，并把"较强且稳定"单列为最高档。

### 实测（沪深300ETF，935 根样本）揭示的结构性发现

| 因子 | IC(10日) | ICIR | 分段为正 |
|---|---|---|---|
| 5日动量 | −0.045 | **−0.88** | 1/7 |
| 20日动量 | −0.026 | **−1.18** | 0/7 |
| 偏离20日均线 | −0.037 | **−0.95** | 1/7 |
| 20日均线斜率 | −0.030 | **−1.13** | 1/7 |
| 快慢线柱 | −0.048 | **−1.01** | 1/7 |
| 真实波幅占比 | **+0.092** | +0.60 | 5/7 |

**动量与趋势类 7 个因子的 IC 全部为负，且分段高度一致（多为 0–1/7 为正）。**

这说明沪深300ETF 存在稳定的**短期反转效应**：涨得多的后面反而弱。

> 这一条直接解释了前面所有关卡的结果：我们那 14 个做多模板里，
> 双均线金叉、均线多头排列、三连阳启动、唐奇安突破、平台突破…… 全是**追涨逻辑**。
> 它们跑不赢买入持有不是因为参数没调好，是**方向本来就是反的**。

已加入自动提示：当动量/趋势类因子 IC 平均 ≤−0.02 时，界面直接指出该标的呈反转特征、
追涨类策略方向是反的。

---

## 四·八·七、2026-08-07 反转假设的检验（含否定结论）

### 起因

因子 IC 显示沪深300ETF 上动量/趋势类因子 **IC 全部为负且分段高度一致**，
推论是"该标的呈反转特征，追涨类策略方向反了"。为检验这个推论，新增**反转组**：
把 7 个追涨模板的进场条件**精确取反**（仍做多），跑五道关卡。

### 结果：反转组也全军覆没，而且更差

| 策略 | 随机百分位 | 样本外超额 | 敏感性 |
|---|---|---|---|
| 均线死叉买入 | **12.0%** | −16.78% | 0/25 |
| 空头排列买入 | 15.5% | −16.78% | 0/25 |
| 破位新低买入 | 18.5% | −16.55% | 0/25 |
| 零轴下死叉买入 | 24.5% | −16.75% | 0/25 |
| 三连阴买入 | 34.5% | −16.71% | 0/25 |
| 月度反转 | 47.0% | −16.70% | 0/25 |
| 跌破平台买入 | 49.0% | −16.46% | 0/25 |

随机百分位**全部低于 50%** —— 这些反转规则的择时比随机进场还差。

### 为什么？分层收益给出了答案

为查明原因，因子面板新增**分层收益**：按因子值从低到高分 5 组，看各组未来 10 日平均收益。

| 因子 | Q1 | Q2 | Q3 | Q4 | Q5 |
|---|---|---|---|---|---|
| 20日动量 | **+1.3%** | −0.0% | −0.2% | +0.2% | +0.2% |
| 偏离20日均线 | **+1.3%** | −0.2% | +0.1% | +0.1% | +0.2% |
| 20日均线斜率 | **+1.3%** | +0.1% | −0.2% | +0.2% | +0.1% |
| 5日动量 | **+1.0%** | −0.2% | +0.4% | −0.0% | +0.3% |
| RSI14 | **+1.1%** | −0.1% | +0.1% | +0.0% | +0.3% |

**规律只存在于 Q1（跌得最惨的那 20%）**：未来 10 日平均 +1.0%~+1.3%。
Q2–Q5 四组全部挤在 −0.2% ~ +0.5% 之间，**彼此几乎没有区别**。

这一次性解释了两件事：

1. **为什么 IC 不大**（|IC| 只有 0.03–0.045）：只有 1/5 的样本有信号，其余 4/5 是噪声，把整体相关性稀释掉了。
2. **为什么反转策略失败**：均线死叉、破位新低这类条件覆盖面**远大于**"最惨的 20%"，
   绝大多数交易落在无效的 Q2–Q5 区间。**它们抓的不是那个信号。**

而且 Q1 的 +1.3% 是 **10 日毛收益**。扣掉往返成本（本工具实测：小资金 0.2%–0.9%），所剩无几。

### 结论

**"IC 为负 ⇒ 反转策略能赚钱"这个推论不成立。**

一个因子有统计上的预测力，和一条交易规则能捕捉到它，是两回事：
- IC 描述的是**整个分布**的单调倾向
- 交易规则只在**特定条件**触发，未必落在有效区间

> 这是本次全部实验里最重要的一条方法论：
> **看到 IC 显著，下一步不是去写策略，而是先看分层收益——
> 确认信号集中在哪个区间，再设计能精确命中那个区间的规则。**

因子面板已加入「分层收益 Q1→Q5」与「Q5−Q1」两列常驻显示。

---

## 四·八·八、因子分位策略：迄今最好的结果，以及它暴露的仓位缺陷

### 做法

因子面板每行新增「建策略」按钮：按该因子的**滚动分位**建仓，
落入最优那一组（IC 为负则取最低 20%）时买入，固定持有 10 个交易日。

**关键防作弊设计**：分位阈值只用「当下往前 250 根」计算，
**绝不使用全样本分位数**——那等于用未来数据决定今天买不买。

### 结果（沪深300ETF）

| 关卡 | 因子·20日动量·最低20% |
|---|---|
| 胜率 | **64.3%**（28 笔） |
| 随机进场百分位 | **87.0%**（迄今最高，仍未过 95 门槛） |
| 参数敏感性 | **25/25 格全正**，当前格 +3.54%，邻域 +2.81% → 稳健 |
| 样本外超额 | −16.54% → 失败 |

**这是全部实验里第一个拿到 25/25 全格正超额的策略。**
敏感性为满仓口径，含义是：满仓运行这个信号，年化可跑赢买入持有 **3.54%**，
且止损/目标在 ±40% 范围内怎么调都是正的。

但它实际年化只有 +0.65%，被买入持有的 +16.79% 碾压。

### 差距的来源：仓位口径不匹配（新发现的缺陷）

策略实际仓位 **6.1%**，买入持有是 100%。满仓的 +3.54% 超额被压缩成 6.1% 的暴露。

而这 6.1% 是**算错的**：凯利公式吃的是「实测胜率 / 赔率」，
那是**任意时点买入**的统计（近 250 次机会），而不是**该策略信号触发时**的统计。
这个策略自己的胜率是 64.3%，明显高于通用统计。

> **仓位是按"随便买"的胜率算的，却用在了一个有选择性的信号上，因此系统性偏小。**

### 为什么没有立刻修

直接改成"用策略自身回测胜率定仓位"会引入**循环论证**：
拿回测结果去给同一段回测定仓位，必然自我强化，样本外会崩。

正确做法是：**用样本内那一半估计胜率/赔率 → 定仓位 → 在样本外验证**。
这需要和现有的样本内外切分打通，属于独立的一项改动，已列为待办 #28。

### 同时要克制的解读

87% 百分位**没有过 95% 门槛**，28 笔样本也偏少。
"25/25 格全正"很漂亮，但敏感性只扫了止损/目标两个维度，
分位窗口（250）、持有期（10日）、分位阈值（20%）这三个参数都没扫过——
它们同样可能是被挑出来的。

**结论：这是目前最值得继续追的一条线索，但还远不到"找到了有效策略"的程度。**

---

## 四·八·九、#28 仓位口径校正 —— 猜测被证实，结论仍是否定

### 做法（严格避免循环论证）

样本内/样本外块新增一行校正：

1. 在**样本内**把该策略按满仓跑一遍，取它**自身**的胜率与盈亏比
2. 由此算半凯利仓位
3. 只在**样本外**用这个仓位验证

估仓位与验证严格分在两段不重叠的数据上——绝不用同一段既定仓位又算收益。

### 结果（沪深300ETF · 因子·20日动量·最低20%）

| | 通用仓位 | 校正后 |
|---|---|---|
| 仓位 | 6.1% | **26.7%** |
| 依据 | "任意时点买入"近 250 次机会的统计 | 样本内该策略自身：胜率 **72.2%**、盈亏比 1.47 |
| 样本外年化 | +0.25% | **+1.41%** |
| 样本外超额 | −16.54% | −15.38% |

**猜测被证实**：通用仓位确实系统性偏小，这里差了 **4.4 倍**。校正后收益提升 5.6 倍。

**但结论没变**：超额仍是 −15.38%，判定依旧失败。

### 为什么修好了仓位仍然跑输：这是算术，不是策略问题

样本外那段买入持有年化 **+16.79%**。而该策略：
- 只在约 **20%** 的时间里持仓
- 持仓时仓位 **26.7%**
- ⇒ 实际市场暴露 ≈ 20% × 26.7% ≈ **5.3%**

**用 5% 的市场暴露去追一个年化 16.79% 的行情，数学上不可能追上。**

> 这条结论适用于所有"择时 + 轻仓"的策略：
> **在单边上涨行情里，它们必然跑输满仓持有。**
> 择时策略的价值从来不在提高收益，而在**降低回撤**——
> 用"超额收益"这一个指标去评判它们，本身就选错了尺子。

这也正好解释了跨标的扫描里那个模式（在跌的标的上跑赢、在涨的标的上大幅跑输）——同一个道理的两种表现。

### 由此产生的新待办

现有的五道关卡全部以**超额收益**为判据，对择时类策略是不公平的。
应补充**风险调整口径**：夏普比率、卡玛比率（年化/最大回撤）、以及"同等回撤下的收益"对比。
已列为待办 #30。

---

## 四·八·十、#30 风险调整口径 —— 同一个策略，两把尺子，两个相反的结论

### 做法

报告最上方（KPI 之下）新增「风险调整口径」，与买入并持有同口径对比：
年化收益、最大回撤、年化波动、**夏普比率**、**卡玛比率**（年化÷回撤），
以及「同等回撤下的等效年化」。

### 结果（沪深300ETF · 因子·20日动量·最低20%）

| 指标 | 本策略 | 买入并持有 | 谁更优 |
|---|---|---|---|
| 年化收益 | +0.52% | **+6.10%** | 持有 |
| 最大回撤 | **−0.54%** | −19.73% | 策略 |
| 年化波动 | **0.54%** | 15.05% | 策略 |
| **夏普比率** | **0.97** | 0.41 | 策略 |
| **卡玛比率** | **0.96** | 0.31 | 策略 |

**同一个策略：在"超额收益"口径下判定失败，在"风险调整"口径下全面胜出（夏普与卡玛均为持有的 2–3 倍）。**

这证实了前面的判断——五道关卡全部以超额为判据，对择时轻仓策略是不公平的尺子。

### 必须同时说清楚的两件事

**① 风险效率高 ≠ 更赚钱。**
年化 0.52% 依然远低于持有的 6.10%。优势完全来自回撤只有 0.54%（持有 19.73%）。
实际含义是"拿着踏实但账户涨得慢"，不是"这个策略更好"。

**② 「同等回撤下的等效年化 +18.92%」是危险数字，已加红色警示。**
它需要把仓位放大 **36.3 倍**。而：
- A 股融资融券最多约 1 倍杠杆（即放大 2 倍），36 倍根本做不到
- 线性换算在高杠杆下严重失效：回撤非线性放大、冲击成本上升、存在强平风险

界面在 lever>2 时会标注「**不可实现**」并给出整段说明。
这个指标的正确用法是**横向比较不同策略的风险效率**，绝不是"照此加杠杆即可获得该收益"。

### 结论

到此，判据体系补完了：**超额收益**回答"赚不赚得多"，**风险调整**回答"值不值得承担"。
两者结论相反时，选择取决于使用者更怕回撤还是更怕踏空 —— 这是价值判断，工具不替人做。

---

## 四·八·十一、#9 / #17 成交可行性（2026-08-07 第十批）

### 涨跌停约束（#9）

回测新增：**一字涨停买不进、一字跌停卖不出**。用不复权价对前收判断，
开盘涨跌幅触及限制（留 0.2% 容差）时：进场信号跳过、离场顺延到下一根。

各类标的的幅度按规则区分，九种情形已逐一核对：

| 标的类型 | 代码特征 | 幅度 |
|---|---|---|
| 沪深主板 | 600/601/603/000/001… | ±10% |
| 创业板 / 科创板 | 300/301/688/689 | ±20% |
| 北交所 | 8/4/92 开头 | ±30% |
| 场内 ETF | 5xxxxx/1xxxxx | ±10% |
| 板块 / 指数 / 场外基金 | — | 不设限（本就不按撮合价成交） |

（ST 股的 ±5% 未建模——数据源不提供 ST 标记。）

### 冲击成本警告（#17）

单笔下单金额超过**当日成交额 1%** 时计数，在报告顶部提示
"真实滑点会明显高于设定值"。这是 LEAN 的 `VolumeShareSlippageModel` 思路的最小可行版本：
不做完整的市场冲击模型，但让使用者知道自己的单子在这一天是不是太大了。

### 实测（宁德时代 300750，本金 500 万）

> ⛒ 成交可行性：**1 次离场因跌停卖不出顺延到下一根**；
> **2 笔的下单金额超过当日成交额 1%**，真实滑点会明显高于设定值。
> （该标的涨跌停幅度按 ±20% 建模）

### #13 状态更正

原条目「默认费率改 0.5‰ 并注明构成」已被 #10/#20 取代：
费率现已拆为 **佣金 2.5‱（每笔最低 5 元）+ 滑点 5‱ + 印花税 5‱（卖出单边）**，
报告副标题常驻标注构成。标记为已完成。

---

## 四·八·十二、#22 ATR 动态止损 + 跟踪止盈（2026-08-07 第十一批）

### 做法

策略表单新增四个风控参数：

- **止损方式**：固定 %（原有）／ **ATR 动态**——止损距离 = 开仓时 ATR(14) × 倍数（默认 1.5），
  开仓时算定、整段持仓不变。波动大时自动放宽，避免被日常噪声扫损
- **跟踪启动 %**：浮盈达到该比例后启动跟踪（填 0 关闭）
- **跟踪回撤 %**：从最高浮盈回撤该比例即止盈离场

退出原因新增「ATR止损」「跟踪止盈」两类，报告副标题常驻标注当前风控配置。

### 受控对比（半导体ETF · 反转组 7 个策略 · 收益% / 最大回撤%）

| 策略 | 固定止损 | ATR 止损 | ATR + 跟踪止盈 |
|---|---|---|---|
| 均线死叉买入 | 0.2% / −2.8% | **1.4% / −2.0%** | 1.3% / −2.1% |
| 空头排列买入 | 5.2% / −1.4% | 5.3% / **−1.1%** | 5.3% / −1.1% |
| 三连阴买入 | −0.3% / −4.6% | **1.0%** / −4.5% | 1.0% / −4.5% |
| 零轴下死叉买入 | 1.7% / −1.3% | **2.1% / −0.9%** | 2.1% / −0.9% |
| 破位新低买入 | −6.5% / −7.1% | **−3.6% / −5.7%** | −5.2% / −7.1% |
| 跌破平台买入 | −1.1% / −2.7% | −0.7% / −2.4% | −0.7% / −2.4% |
| 月度反转 | −1.0% / −4.9% | 0.0% / −5.6% | −0.4% / −5.7% |

**统计：ATR 止损在 6/7 个策略上降低了回撤，7/7 个策略上提高了收益。**

方向符合设计预期：固定百分比止损在高波动标的上过于紧凑，会被日常噪声频繁扫出；
ATR 按标的自身波动定距离，减少了这类无谓止损。

**但要克制解读**：这是同一段历史上的比较，7 个样本、单一标的，
且这批策略本身都没通过前面五道关卡。它只说明"ATR 止损比固定止损更贴合波动"，
**不说明这些策略变得可用了**。

---

## 四·八·十三、#29 因子参数敏感性 —— 推翻了自己上一轮的"最好结果"

### 做法

因子策略的三个核心参数（分位窗口 / 持有期 / 分位阈值）此前是写死的全局常量，
从未被扫描过。已改为随策略携带，敏感性面板对因子策略自动切换扫描维度：

- 热力图：**持有期（5/8/10/15/20 交易日） × 分位阈值（10/15/20/25/30%）** 共 25 格
- 另附：**分位窗口** 120 / 250 / 400 根 三档

### 结果：之前的"稳健"是假象

同一个策略（因子·20日动量·最低20%，沪深300ETF）：

| 扫描维度 | 结果 | 判定 |
|---|---|---|
| 止损 × 目标（旧） | **25/25 格全正** | 稳健 ✔ |
| **持有期 × 分位阈值（新）** | **12/25 格为正**，中位 **−0.50%**，邻域均值 +0.65% | 偏弱 ～ |

分位窗口一维更直白：

| 窗口 | 满仓超额 |
|---|---|
| 120 根 | **−0.45%** |
| **250 根（当前）** | **+3.54%** |
| 400 根 | +1.58% |

**当前这组参数（250/10/20%）恰好是三档里最好的那一档，换成 120 根就转负。**

### 教训

上一轮"25/25 全格正超额"之所以好看，是因为**扫的两个参数对这个策略几乎不起作用**——
固定持有 10 天的策略，止损/目标很少被触及（这一点当时的低方差检测其实已经提示过）。
**真正决定它表现的三个参数，一个都没扫。**

> **敏感性检验只有扫对参数才算数。**
> 扫一堆无关参数得到的"稳健"，比不扫更有欺骗性——因为它给了一个虚假的通过标记。

判定已正确降级为「参数选择仍在影响结论」。至此，**先前那个"迄今最好的结果"不再成立**。

---

## 四·八·十四、#21 组合回测 —— 从"择时"转到"选品种"

### 做法

新增**多标的组合回测引擎**（原引擎是单标的单持仓，这是结构性新增）：

1. 取当前分类常用标的（16 个）的**公共交易日**构建对齐面板
2. 用选定因子给每个标的算滚动分位打分
3. 每 **10 日**调仓，持有得分最好的 **3 只**，等权
4. 约束：单票上限 40%、总仓上限 95%、卖出后 **5 日冷却**不可重买
5. 费用照旧：佣金（最低5元）+ 滑点 + 印花税 + 100 股整手
6. 基准：**等权持有全篮子**

入口在因子面板每行的「组合」按钮。

### 结果（沪深300ETF 分类篮子 · 20日动量因子 · 2023-07 → 2026-08）

| 指标 | 因子选股组合 | 等权持有全篮子 |
|---|---|---|
| 年化收益 | **+9.09%** | +8.78% |
| 最大回撤 | **−22.72%** | −23.87% |
| 年化波动 | **18.63%** | 19.61% |
| 夏普 | **0.49** | 0.45 |
| 卡玛 | **0.40** | 0.37 |

**五项全部小幅占优** —— 这是全部实验里第一次出现"策略在所有维度都不输"的情况。
此前所有择时策略都是拿收益换回撤，这次收益和回撤同时略好。

**但超额只有 0.31%/年**，而三年里发生 293 次买卖、付出 5572 元费用。
判定为「优势不明显，可能来自换仓摩擦与样本波动」。

### 为什么这个方向比择时更值得追

前面所有关卡测的都是**择时**（什么时候买同一个标的）。
组合回测测的是**选品种**（同一时间买哪几个），这是一条独立的证据线。

理论上也更站得住：择时要求预测时间序列的方向（极难），
选品种只要求**相对排序**正确（哪个比哪个强），后者的信噪比通常高一个量级。

### 已知局限（界面已常驻标注）

- 篮子只含**今天仍在市**的标的，存在幸存者偏差
- 调仓周期 / 持仓数 / 单票上限 / 冷却期**均为固定值，尚未做敏感性扫描**
  —— 吸取 #29 的教训，在扫过这些参数之前，不能称这个结果"稳健"
- 未做样本内外切分与随机基准对照（对应待办 #31）

---

## 四·八·十五、#31 组合三关 —— 上一轮那个"五项全占优"同样没通过

### 三道关卡

组合回测结果下方自动追加：

1. **随机选股基准**：120 组随机挑 K 只，调仓节奏与全部约束完全相同，只把"选哪几只"换成随机
2. **样本内 / 样本外**：面板日期 70/30 切分，各自与同期等权基准比超额
3. **参数敏感性**：调仓周期（5/10/15/20/30 日）× 持仓数（1/2/3/5/8 只）共 25 格

### 结果（沪深300ETF 篮子 · 20日动量因子）

| 关卡 | 结果 | 判定 |
|---|---|---|
| ① 随机选股 | 第 **92.5** 百分位（随机中位 −0.05%，本组合 +9.02%） | 未过 95 ～ |
| ② 样本内外 | 超额 **+10.39%/年 → −25.27%/年** | ✖ |
| ③ 参数敏感性 | **25 格仅 5 格**跑赢等权 | ✖ |

参数平面（超额%，行=持仓数，列=调仓 5/10/15/20/30 日）：

```
持仓1只:  -10.3   -5.9   -9.3   -7.8   -7.3
持仓2只:  -12.9   +0.8  -10.0   -5.8   -3.3
持仓3只:  -14.6   +0.2   -8.8  -11.6   -2.9   ← 当前(10日/3只)=+0.24
持仓5只:   -9.3   +1.9   -1.9   -7.0   +1.7
持仓8只:   -7.0   -1.5   -2.2    ...
```

**整个平面几乎全负，正值集中在"调仓 10 日"这一列** —— 而 10 日恰好是默认值。
这是孤立列，不是稳健区域。

样本内外那一关最致命：**样本内 +10.39%/年，样本外 −25.27%/年**。
这是最典型的过拟合形态——在能看到的历史上大幅领先，在没看过的数据上大幅落后。

### 方法论上的一点进步

上一轮看到"收益/回撤/波动/夏普/卡玛五项全线小胜"，判了 weak 还觉得偏保守。
这次**先建关卡、再下结论**，避免了重复 #29 那次的错误（先宣布"最好结果"，扫对参数后被推翻）。

> 规律：**任何新方向的第一个好结果，都应假定它没通过，直到关卡说它通过。**

### 当前总账

至此，已检验的方向全部未通过：

| 方向 | 结论 |
|---|---|
| 14 个追涨模板 | 跑输买入持有，随机基准多在 50% 以下 |
| 7 个反转模板 | 更差，随机百分位 12%–49% |
| 因子分位择时 | 扫对参数后 12/25，窗口一换就转负 |
| 因子选股组合 | 样本外超额 −25.27%，参数 5/25 |

**没有找到任何能通过全部关卡的策略。**

---

## 四·八·十六、#32 资产配置 —— 换一条不需要预测能力的路

前面 4 行总账说的都是同一件事：**我们没有预测能力。**
但"没有预测能力"不等于"什么都做不了"。资产配置这条路的前提恰恰是**承认自己预测不了**，
然后只回答三个可以被历史验证的问题：买几只、多久调一次、分几次买。

界面：主面板 `◈ 资产配置` → 弹窗 `▶ 开始`。篮子固定为四只宽基 ETF
（沪深300 510300、中证500 510500、创业板 159915、科创50 588000），取公共交易日区间。

### 实测结果（2022-06-27 → 2026-08-07，1000 个交易日，含真实费用）

**① 宽基组合：分散确实削减回撤，但削得很少**

| | 年化 | 最大回撤 | 波动 | 夏普 | 卡玛 |
|---|---|---|---|---|---|
| 沪深300ETF | +2.95% | −23.73% | 14.90% | 0.20 | 0.12 |
| 中证500ETF | +6.92% | −30.20% | 21.75% | 0.32 | 0.23 |
| 创业板ETF | +6.75% | −45.83% | 31.80% | 0.21 | 0.15 |
| 科创50ETF | +12.06% | −45.16% | 33.82% | 0.36 | 0.27 |
| **等权 · 不调仓** | +7.33% | −34.59% | 23.73% | 0.31 | 0.21 |
| **等权 · 每季度再平衡** | +7.59% | −35.00% | 24.07% | 0.32 | 0.22 |

单只平均回撤 36.23%，等权组合 35.00%，**只削减了 1.23 个百分点**。
原因很直白：A 股这四只宽基高度同涨同跌，分散的前提是相关性低，而它们没有。
夏普从单只平均 0.27 提升到 0.32——**这是真实的、不需要任何择时能力的改善，但幅度诚实地说很有限。**
季度再平衡这一次是加分的（+0.26 个百分点年化），但它本来就不保证提高收益，
主要作用是把仓位拉回目标、避免涨得多的那只越占越大。

**② 定投模拟：把"买在哪一天"这个变量摊平**

每 20 个交易日投 2000 元 × 50 次，累计 91091 元实际投入：

| | 期末 | 收益 |
|---|---|---|
| 定投 | 109599 | **+20.32%** |
| 期初一次性全投 | 102591 | +12.63% |
| 最幸运时点一次性（2024-02-02） | 132914 | +45.91% |
| 最倒霉时点一次性（2026-06-22） | 85857 | −5.75% |

**择时的价值区间是 51.66 个百分点**（−5.75% ~ +45.91%）。这是"选对买入日"这一件事的全部上限。
定投拿到 20.32%，落在这个区间的**第 50 百分位**——正好是中位数，这不是巧合，是定投的定义。
定投的意义不是收益最高，而是**放弃了最好和最坏两端，换来结果不取决于运气**。

费用上有个对小账户很重要的发现：累计费用 250 元，占总投入 0.274%，
**每笔最低 5 元佣金在小额定投下占比显著**——每次 2000 元时，5 元就是 0.25%，
一年 12 次相当于白扣 3%。调高单次金额或降低频率能直接省下这块。

**③ 有效前沿：这张图的正确读法是"看形状"，不是"抄最优点"**

3000 组随机权重的收益/风险散点。最大夏普配比落在
中证500 37% + 科创50 63%（年化 +10.43%，回撤 −39.13%，夏普 0.37），等权是 0.32。

**这个"最优配比"是在已知历史上算出来的，本身就是过拟合。**
换一段时间最优点必然移动，直接照抄没有意义——它等价于"买过去四年涨得最好的那两只"。
这张图真正有用的是散点云的形状：上边缘几乎是一条直线，说明在这个篮子里
**多承担风险和多拿收益基本是等价交换，没有免费午餐区间**。
等权点离上边缘不远，也就是说复杂的配比优化在这里换不来多少东西。

### 这一项和前面十五项的区别

前面每一项都在问"能不能预测"，答案全是不能。
这一项不问预测，问的是"在承认预测不了的前提下，还能拿到什么"。
答案是：**回撤削减约 1.2 个百分点、夏普提升约 0.05、以及买入时点风险从 ±26 个百分点降到 0。**
不多，但每一项都经得起复现，且不依赖任何我们已经证伪的能力。

---

## 四·八·十七、#33 美股标的库 —— 为什么没走支付宝/同花顺

### 数据源选择

你提到从支付宝或同花顺抓。这两条路我都没走，原因是硬的：

| 来源 | 问题 |
|---|---|
| 支付宝 | 行情在小程序里，走的是加密的私有协议 + 设备指纹，要抓必须逆向客户端并登录你的账号。我不会登录你的账号，也不做协议逆向 |
| 同花顺 | 网页端行情接口带 `hexin-v` 动态签名，签名算法在混淆过的 JS 里，且有 IP 风控。抓取属于绕过其反爬机制 |
| **东方财富**（已采用） | 就是我们现在用的同一套接口、同一个域名。美股列表 `fs=m:105,m:106,m:107`，日线 `secid=105.AAPL`，快照 `stock/get`，**参数结构和 A 股完全一致，无需登录、无需签名** |

实测东方财富美股接口全部可用：三个市场共 **13718 只**，日线支持 `fqt=0/1/2`（不复权/前复权/后复权），
快照返回 `f59=3`（美股三位小数）。也就是说我们现有的双序列复权机制、实时快照机制**一行都不用改就能复用**。

### 但直接把美股塞进去会静默算错四件事

这才是这一项真正的工作量。如果只加清单不改规则，回测会在你看不见的地方出错：

| 项 | A 股 | 美股 | 不改会怎样 |
|---|---|---|---|
| 最小交易单位 | 100 股整手 | **1 股** | 高价股（如 COST 944 美元）按 100 股算要 9.4 万美元一手，10 万本金几乎全被"资金不足一手"跳过 |
| 印花税 | 卖出 5‱ | **无** | 每笔多扣 0.05%，高频策略被凭空吃掉几个点 |
| 涨跌停 | ±10/20/30% | **无**（熔断是全市场级，不建模） | 会误判"一字涨停买不进"，凭空跳过信号 |
| 计价货币 | 元 | **美元** | 报告里"累计费用 133 元"实际是 133 美元，差 7 倍 |
| 交易时段 | 09:15–15:05 本机时间 | **09:30–16:00 美东** | 实时同步永远显示"休市" |

做法：新增 `isUS(d)` 按东财市场号（105/106/107）识别，`mkt(d)` 返回该市场的
`{lot, stamp, minComm, cur, unit}`，把 `LOT / STAMP / MIN_COMM` 三个原本写死的 A 股常数
从回测引擎、组合引擎、定投模拟三处全部换成按标的取值。
交易时段用 `toLocaleString(..., {timeZone:'America/New_York'})` 换算，夏令时自动处理。

### 实测验证

**美股（苹果 AAPL，1000 根日线，2022-11-14 → 2026-08-07）**

```
报告页：苹果 ｜ 做多 ｜ 仓位 9.2% ｜ 佣金 2.5‱(最低1美元) + 滑点 5.0‱ ｜ 美股无印花税 ｜ 1股起买
累计费用 133 美元（占初始资金 0.13% · 均 5 美元/笔）
成交明细：60 股 / 58 股 …（非 100 的倍数 → 1 股起买生效）
```

**A 股回归验证（沪深300ETF）**

```
报告页：佣金 2.5‱(最低5元) + 滑点 5.0‱ + 印花税 5‱(卖出) ｜ 100股整手
成交明细：1600 / 1400 / 1500 股（全部 100 的倍数 → 未受影响）
```

顺带一个**不是 bug 的正确行为**值得记一笔：茅台 1309 元/股，一手 13.09 万，
超过默认 10 万本金，引擎报"47 次因资金不足一手被跳过"——这是对的，
A 股 100 股整手对小账户买高价股就是这个结果，工具如实告诉你比悄悄放行更有用。

### 三点必须说清的局限

1. **行情有延迟。** 东方财富的美股快照通常延迟约 15 分钟，不是实时逐笔。做日线回测没问题，做日内没意义。
2. **佣金口径是猜的。** A 股的"最低 5 元"是行业惯例，美股各券商差异极大（IB 约 1 美元、
   老虎/富途另有平台费、部分美国券商零佣金）。我取了 1 美元这个常见下限，**偏乐观**。
   你实际用哪家，把这个数改掉再看结论。
3. **不含汇率与汇兑成本。** 全程按美元计价，没建模换汇点差和额度限制。
   如果你是用人民币换汇去买，真实收益要再打一道折扣。

### 一个提醒

标的库从 35507 涨到 **49270** 个，单文件 HTML 从 2.79 MB 涨到 3.26 MB。
多出来的一万三千只美股里，绝大多数是仙股、ADR、杠杆 ETF 和期权收益策略基金
（搜 "TSLA" 会同时搜出 `TYYY`、`TSII` 这类衍生产品）。
**能搜到不等于值得买**——这一点在美股比 A 股严重得多，因为美股没有涨跌停、退市极快。

---

## 四·八·十八、卡死 Bug：一个浮点误差引起的死循环

### 症状

点「推演」「按市况匹配」「生成 10 个策略」后 K 线图卡住，甚至整个网页无响应。

### 定位过程

第一次测量（沪深300ETF）显示这三个按钮的同步阻塞分别是 72ms / 5ms / 0ms，看起来完全正常——
**但这个测量是无效的**：策略卡有去重逻辑，第二次点击时所有模板都被跳过，等于什么都没算。
清空后重测仍然只有 50ms 的长任务。

换到**场外基金**（易方达消费 110022）后立刻复现：渲染进程直接挂死，
连 `navigate` 都超时 300 秒。逐个按钮二分，确认是「生成 10 个策略」触发，「推演」单独点是好的（87ms）。

### 根因

回测引擎的开仓函数 `openPos` 用**迭代逼近**来算能买多少：

```js
var sh = otc ? budget / px : Math.floor(budget / px / LOT) * LOT;
while (sh > 0) {
  var amt = sh * px;
  var f = otc ? amt * buyFeeOtc : Math.max(amt * comm, MIN_COMM);
  if (amt + f <= budget || dir === -1) return {...};
  if (otc) { sh = budget / (px * (1 + buyFeeOtc)); continue; }   // ← 这里
  sh -= LOT;
}
```

场内分支每次 `sh -= LOT`，单调递减，必然终止。
**场外分支不减少 `sh`，而是重新赋一个固定值**——它假设 `budget/(1+费率)` 再加上费用一定正好等于 `budget`。
数学上成立，浮点上不成立：`sh*px` 和 `amt*bf` 各带一次舍入，`amt + f` 可能比 `budget` 大一个 ULP，
于是条件不满足 → 重新算出**完全相同的 `sh`** → 无限循环。

数值实验（申购费 0.15%，58120 组预算×价格组合）：

```
二次迭代后 amt+f 仍 > budget 的比例：4.74%（2755 例）
典型超出量：2.27e-13
```

**4.74% 的命中率**意味着：1000 根日线 × 13 个场外可用策略，撞上一次几乎是必然的。
所以症状是"点了就死"，而不是偶发。

这解释了用户报的全部三个现象——**一旦渲染进程被死循环占住，
K 线图、推演面板、整个页面都会同时冻结**，看起来像是三个按钮各自有问题，其实是同一个。

### 修复

改成**直接解算，不迭代**：

- 场外：`amt = budget/(1+申购费)`，`fee = budget - amt`。这样 `amt + fee` 与 `budget` **按定义完全相等**，不依赖浮点巧合。
- 场内做多：约束 `amt + max(amt·佣金率, 最低佣金) ≤ budget` 等价于两条线性约束同时成立
  （因为 `max(a,b) ≤ X ⟺ a ≤ X 且 b ≤ X`），取较紧的一条直接算出上限再向下取整到整手。
  另留最多回退 2 手的**有界**兜底循环。
- 场内做空：开仓即卖出，不受预算约束，直接返回。

顺带解决了一个潜在的性能问题：原来的 `sh -= LOT` 在美股（`LOT = 1`）买低价股时，
要一股一股地退，单次开仓可能循环上千次。现在是 O(1)。

### 验证

```
48200 组 (价格, 预算) × 两种市场规则（A股100股/5元、美股1股/1美元）：
  新解算式与旧迭代式手数不一致  0 例
  新解算式超出预算              0 例
场外 1453 组：amt+fee 超预算    0 例
```

浏览器端：场外基金上连点「推演/按市况匹配/生成10个/反向做空/反转组」11 次，
单次阻塞 7–57ms，**掉帧 0 次**，打开回测报告（跑完五道关卡）153ms。
A 股回归：沪深300ETF 成交仍是 1500/1400/1400/1500 股（100 整手），
美股：苹果 62/52/54/40 股（1 股起买），**数字与修复前一致**。

### 教训

这个 bug 在代码里躺了很久，一直没暴露，因为**开发时主要在场内标的上测**。
用户说过"我现在主要关注基金"——而场外基金正好是唯一走那条分支的路径。
测试覆盖要跟着用户的实际使用路径走，不是跟着开发者的顺手路径走。

---

## 四·八·十九、K 线图"卡住"：上一轮修错了地方，真因是另外两个

上一轮我修的死循环是真 bug，但**不是用户说的这个 bug**。用户复述"推演导致 K 线图卡住、无法切换、
和 K 线图有关的操作都不行"之后重查，找到两个此前一直没暴露的问题。

### 真因一：ECharts 在 0 尺寸容器上初始化，此后永远画不出东西

诊断的关键一步是**不看代码、直接量像素**：

```
容器 #chart 盒子   905 x 311.5     ← DOM 布局正常
ECharts 实例宽高   0 x 0           ← 但图表记的是 0
canvas             0 x 0
getDataURL()       抛错：canvas with a width or height of 0
```

`echarts.init()` 会把容器宽高**记死**。如果那一刻容器还没完成布局，它就永久记成 0×0
（或 `resize()` 时退化成 100×100），此后无论 `setOption` 多少次都画不出任何东西。
现象正是"图卡住不动、换标的没反应、点推演/播放/重置都没用"，
而**手动改一下窗口大小就恢复正常**——这也解释了为什么这个问题时有时无、难以复现。

什么时候会 0 尺寸初始化？最主要的是**页面在后台标签页里加载**。
exe 启动时调用默认浏览器打开页面，如果浏览器窗口尚未显示或标签页在后台，
首次布局就可能是 0。

### 一个反直觉的点：ResizeObserver 救不了它

我第一版修复只挂了 `ResizeObserver`。测试时发现它**一次都没回调**——
把容器从 905 改到 0 再改回 905，回调序列是空的。

原因是：**ResizeObserver 的回调和 `requestAnimationFrame` 一样，属于渲染生命周期的一部分，
页面不渲染时（后台标签页、窗口隐藏）会被浏览器一起挂起。**
而"后台标签页"恰恰就是产生 0 尺寸初始化的主要场景——
**需要它工作的时候，它正好不工作。**

（这个项目早期还踩过一次同源的坑：数字滚动动画用 rAF，面板隐藏时被挂起导致数字卡死，
当时的解法是加 `setTimeout` 兜底。同一个道理。）

所以最终方案是三层，真正兜底的是第一层：

1. **`syncSize()`：每次重绘前直接比对容器实际尺寸，同步执行，不依赖任何异步回调。**
   `renderChart()` 开头调用一次——这样推演、播放、重置、换标的，任何一个操作都会顺手把图治好。
2. `ResizeObserver`：页面可见时处理正常的布局变化（拖窗口、F11、缩放）。
3. `visibilitychange` / `pageshow`：标签页从后台切回前台时重新量一次。

判据必须是"和容器尺寸不一致"，不能写成 `if (!getWidth())`——
因为 ECharts 对 0 尺寸容器**有时记 0×0，有时退化成 100×100**，后者是 truthy，会被漏掉。

验证（人为把实例尺寸打坏成 100×100 再执行各操作）：

```
① 基准        → 905x312  出图 67050B
② 制造错乱     → 100x100  出图  6038B   （坏图）
③ 点推演      → 905x312  出图 70134B   ✓ 自愈
④ 关推演      → 905x312  出图 67066B   ✓
⑤ 错乱后重置   → 905x312  出图 32206B   ✓
⑥ 错乱后换标的 → 905x312  出图 91438B   ✓
⑦ 场外开推演   → 905x312  出图 92938B   ✓
```

### 真因二：回测报告的 HTML 少了一个 `>`

```html
<div id="rndBox"          ← 少了 >
  <div class="rh">
```

HTML 解析器把后面的 `<div class="rh">` 整个当成了 `rndBox` 的属性吃掉，
于是 `.rh` 这个元素凭空消失，本该关闭它的 `</div>` 转而关掉了 `#rndBox`，
错位一路传染到 `#modalBody` **提前闭合**。

后果是：`#oosBox`、`#sensBox`、`#scanBox`、净值图、交易明细表全部变成 `#modal` 的直接子元素，
而 `#modal` 是 `display:flex; flex-direction:column; max-height:88vh` ——
它们被 flex 一起压扁：

| 元素 | 应有高度 | 实际 |
|---|---|---|
| `#modalBody`（可滚动区） | 撑满 | **24px** |
| `#eqChart`（净值曲线） | 170px | **4px** |
| `#tradeTbl` 外层（交易明细） | 300px | **2px** |
| `#rndBox`（随机基准） | 174px | **36px** |

也就是说**打开回测报告后，净值曲线是一条 4 像素的缝，明细表 2 像素，整个弹窗还滚不动**。
这一条同样落在用户说的"和 K 线图有关的操作"范围里。

补上 `>` 之后 DOM 树恢复正常：`#modal` 只剩 header / modalBody(545px, 内容 1721px 可滚动) / 免责声明
三个子元素，各区块高度 275 / 172 / 264 / 225 / 60 / 167 / 276，全部正常。

顺手写了个标签配平检查扫全文件：**175 个 `<div>` 对 175 个 `</div>`，无其它未闭合标签。**

### 这一轮的教训

上一轮我拿"场外基金死循环已修复 + 11 次连点零掉帧"就宣布问题解决了，
但那些测量全是在**读 JS 状态**（series 长度、x 轴数组、耗时），
**没有一次去量图表真实的像素尺寸，也没有把图真的渲染出来看一眼**。
状态对不等于画面对——只要早一步调 `getDataURL()`，第一轮就能发现。

以后凡是"界面卡住/显示不对"类的问题，第一步是量像素、出图，不是读状态。

---

## 四·八·二十、演练时主线程被打满 + 推演面板改为悬停展开

用户反馈：播放 K 线后反复点推演仍会卡；另外推演弹出的文字挡住了太多图。

### 卡顿真因：定时器间隔比一次重绘还短

演练用的是 `setInterval(tick, 速度值)`，而 `<select id="speed">` 的四档值直接就是毫秒间隔：
**120 / 60 / 24 / 8**。一次 tick 要做 `renderChart()` + `paintSignals()`：

- `renderChart()` 重建 6 条推演序列并 `setOption` 整张 1000 根 K 线 —— 实测 **40–70ms**
- `paintSignals()` 里的胜率/赔率是 250 根首触模拟，也不便宜

**15 倍速时间隔 8ms，而一次 tick 要 40ms 以上——任务永远排不完，回调不断堆积，主线程被打满。**
这时点任何按钮（推演首当其冲，因为它又是一次整图重绘）都得排在队尾，表现就是"点了没反应/卡住"。

量化证据（每 4ms 打一次点，看实际被延迟到多少）：

| 场景 | 中位间隔 | p90 | 最大 | 2.5 秒内采样数 |
|---|---|---|---|---|
| 空闲 | 4ms | 5ms | 7ms | 375 |
| 播放 2 倍速 | 4ms | 28ms | 42ms | 358 |
| 播放 2 倍速 + 推演 | 4ms | 29ms | 44ms | 331 |
| **播放 15 倍速 + 推演** | **37ms** | **69ms** | **85ms** | **55** |

采样数从 375 掉到 55，就是"主线程只剩 1/7 的时间片给界面"。

### 修法

**1）链式定时器 + 强制留白（关键）**

把 `setInterval` 换成"跑完一次再排下一次"，且下一次的延迟不小于本次的开销：

```js
var cost = performance.now() - t0;
play.timer = setTimeout(tick, Math.max(step - cost, Math.min(cost, 60)));
```

`Math.min(cost, 60)` 这一项保证**主线程占用率不超过 50%**（上限 60ms 空闲），
无论用户把速度调到多快，界面永远留得出响应时间。

**2）跟不上就跳根，不排队**

按真实流逝时间算该走几根，一帧内一次性推进（上限 8 根）但**只重绘一次**。
这样倍速观感不变，重绘频率却降到机器扛得住的水平。

**3）演练中胜率/赔率每 4 根刷一次**

250 根首触模拟逐根重算既贵又没人看得过来；暂停和跑完时仍会立即刷新一次，保证最终值准确。

**4）连点推演合并成一次**

按钮状态立刻翻（即时反馈），真正的重绘与日志延后 80ms 执行，期间的新点击会取消上一次。
连点 15 次的同步耗时从"15 × 一次整图重绘"降到 **1ms**。

修复后同一组测量：

| 场景 | 中位 | p90 | 采样数 |
|---|---|---|---|
| 播放 15 倍速 + 推演 | **4ms** | **5ms** | **302**（原 55） |
| 连点推演 12 次 | 同步 **0ms** | — | — |

另验证 15 倍速能正常跑到底：1000/1000，用时约 18 秒，按钮自动复位成「▶ 播放」，无报错。

### 推演面板改为悬停展开

原来推演一开，右上角就常驻一个 186×167 的数据面板，**占图表面积的 11.04%**，一直压着 K 线。

改成：开启后只留一枚 **111×19 的小角标**「◈ 推演中 · 悬停看数据」，**占比 0.75%**；
指针停在角标上才淡入完整数据，移开即收起。数据框设 `pointer-events:none`，不会挡住图表交互。

结构上把角标和数据框包进 `#projHover`，悬停判据是这个小容器而不是整个图表面板——
否则鼠标只要在图上就展开，等于没改。

---

## 四·八·二十一、推演点击延迟：我上一轮的"修复"自己就是病因

用户说点推演后 K 线图还是顿。这一轮先量成本再动手，结论有点难堪：**上一轮加的防抖就是病因。**

### 第一步：把重绘成本拆开量

在实例上做对照实验（1000 根日线，每种重复 12 次取均值）：

| 做法 | 耗时 |
|---|---|
| A 全量 10 条 series + xAxis（现状） | 28.1ms |
| B 只发 6 条推演序列（其余传空对象） | 22.4ms |
| C 只发 K 线 1 条 | 19.6ms |
| **E 只改 dataZoom，一条数据都不发** | **19.0ms** |

**连"一条数据都不发"都要 19ms。** 这一条排除了整个"稀疏更新/增量传数据"方向——
成本不在数据传输，而在 setOption 触发的整张画布重绘，省数据是省不掉的。

继续量影响重绘的因素：

| 条件 | 耗时 |
|---|---|
| 基线 | 28.2ms |
| 关动画 | 24.3ms（本来就是 `animation:false`，说明这 4ms 是测量噪声） |
| **K线/量能开 `large` 模式** | **19.7ms** |
| 可见窗口 130 根 | 16.1ms |
| 可见窗口 1000 根 | 30.2ms |

又量了交互路径，排除另一个方向：推演开启后悬停 1.4ms/次、拖缩放 9.8ms/次，
**都不比关闭时慢**（1.8ms / 13.6ms），所以不是交互变重。

### 第二步：量端到端延迟，找到真凶

前面全是单次成本，20–28ms 一次抖动不该被感知成"卡顿"。于是改量**从点击到图真正变化**：

```
第1次点击→图变化 129ms
第2次点击→图变化 142ms
第3次点击→图变化 124ms
```

124–142ms。人对"点了就动"的感知阈值大约是 100ms，超过就明显觉得顿。
其中 **80ms 是上一轮我为了合并连点加的 `setTimeout(applyProj, 80)`** ——
我把它写成了**纯延后触发**，结果单次点击也要白等 80ms。
上一轮我只测了"连点 12 次同步耗时 0ms"就收工，**从没测过单次点击的端到端延迟**。

### 落地方案（两处，都已验证）

**1）防抖改成前沿触发**

首次点击同步执行，只有 120ms 内的后续连点才合并成一次：

```js
var now = performance.now();
clearTimeout(projTimer);
if (now - projLast >= 120) { projLast = now; applyProj(); }        // 首次立即
else projTimer = setTimeout(function () { projLast = performance.now(); applyProj(); }, 120);
```

**2）K 线与量能柱开 `large` 模式**

```js
{ name: 'K线', type: 'candlestick', large: true, largeThreshold: 200, ... }
{ name: '副图', type: 'bar',        large: true, largeThreshold: 200, ... }
```

代价是不做逐根 hover 高亮——而本项目的 tooltip 是 `trigger:'axis'` 按坐标轴触发的，
不依赖逐根高亮，所以实际没有功能损失。渲染结果已出图逐项核对：红涨绿跌、均线、
成交量、推演扇形带全部正常。

### 结果

| 指标 | 修复前 | 修复后 |
|---|---|---|
| **点击 → 图变化** | 124–142ms | **27–30ms** |
| 整图重绘 | 28.1ms | 21.5ms |
| 播放 2 倍速 + 推演（4ms 打点的中位延迟） | 4ms / 样本 358 | 4ms / **样本 494** |
| 播放 15 倍速 + 推演 | 中位 37ms / 样本 55 | 中位 **4ms** / 样本 **340** |
| 播放中连点推演 20 次 | — | 同步 **17ms** |

### 教训（第二次栽在同一件事上）

上一轮的教训是"界面问题要量像素，不要读状态"。这一轮是它的延伸：
**性能问题要量用户真正感知的那个量。**

"连点 12 次同步耗时 0ms"是个漂亮但无关的数字——它证明的是连点被合并了，
而用户抱怨的是**单次点击的响应速度**，那正是被我的合并逻辑拖慢的东西。
优化动作本身可能就是新瓶颈，改完必须回头测原来那条路径。

---

## 四·八·二十二、推演拆成独立窗口（用户提的方案，是对的）

前面两轮我一直在**优化"推演叠在主图上"这件事的成本**：节流、防抖、前沿触发、large 模式。
用户直接说：别接在 K 线图上，另开一个窗口放推演数据和推演线图。
这是把问题**消除**而不是优化 —— 主图根本不需要知道推演的存在。

### 原架构的固有开销

推演是 6 条 series（p5 堆叠基线 / 90%区间带 / 25%分位 / 75%分位 / 中位 / 回归线）
叠在主 K 线图上。开关一次意味着：

1. 重算 6 条长度等于类目轴的数组
2. 把 x 轴向右扩 20 根未来交易日（1000 → 1020），**类目轴长度一变，整图必须重新布局**
3. 所有历史序列补齐到新长度
4. 一次 `setOption` 重绘 1000 根 K 线

前一轮实测过：**哪怕一条数据都不发、只改 dataZoom，也要 19ms** —— 这个成本是"动主图"本身的，
只要推演还挂在主图上，就绕不过去。

### 新结构

- 新增 `#mask4` / `#modal4`「◈ 概率区间推演」窗口，内含自己的 ECharts 实例 `#pjChart`
- 图：左边最近 60 根**真实走势**，右边 20 根分位扇形（90% 区间带 + 25/75 虚线 + 中位点线 + 回归线）
- 数据：中位 / 50%区间 / 90%区间 / 上涨概率 / 年化波动 / 回归斜率，六张卡片，不再挤在角落
- `btnProj` 从"切换主图序列"变成"打开窗口"；另有「↻ 按当前进度重算」按钮
- 主图的 `baseOption` 删掉那 6 条 series，`renderChart()` 删掉 x 轴扩展与补齐逻辑

`renderChart()` 从 46 行降到 15 行，主图 series 从 10 条降到 4 条。

### 验证：主图指纹前后完全一致

```
主图指纹(推演前)  1000|4|K线,20日均线,副图,净值
点推演                47ms（首次创建推演图实例）
主图指纹(推演后)  1000|4|K线,20日均线,副图,净值
主图是否被改动     否 ✓ 完全未触碰
```

| 指标 | 叠在主图时 | 独立窗口后 |
|---|---|---|
| 重复开推演 | 每次整图重绘 20–28ms | **13–20ms**（只画推演窗口那张小图） |
| 播放 15 倍速时开关推演 10 次 | — | 242ms（每次开+关 24ms） |
| 播放 15 倍速主线程中位延迟 | 4ms | **4ms**（不受推演影响） |
| 推演对 K 线图的开销 | 每次整图重绘 | **0** |

### 顺带修掉一个被掩盖的错误：推演显示的是复权价

搬家后美股一测就露馅：英伟达现价 223.96，推演中位却显示 **111458**。
因为推演全程用 `D.c`（后复权序列），A 股复权系数小、看着还算合理，
美股经历过拆股，后复权价和真实价差了三个数量级。

改法是分开口径：**漂移 μ 与波动 σ 仍取复权序列**（分红不能算成下跌，这是对的），
但 `S0`、历史线、回归线全部换成真实市价 `D.rc`，回归线按锚点比例换算。
修完：英伟达 223.96 → 中位 231.70；沪深300ETF 4.751 → 中位 4.761，与顶栏现价对得上。

### 一次自己造成的事故，记在这里

改这一版时我用「从 `paintProjBox` 开头切到 `applyProj` 结尾」的方式整段替换，
但这两个函数在文件里**隔着几百行**，中间夹着 `renderChart`、`startPlay`、`stopPlay`、
`chartData`、resize 处理等等，一刀下去全删了。

发现方式是一个逐项存在性检查（`renderChart` 0、`startPlay` 0、`chartData` 0 …）。
恢复方式是从上一次构建产物 `QUANT_ENGINE_v10.html` 里把内联的 app.js 抠出来还原 —— 
**构建产物意外地充当了一次备份**（这个项目没有 git）。

重做时改成按锚点行做**有界替换**，并在脚本末尾加了强制自检：

```python
for f in ['function renderChart(', 'function startPlay(', 'function chartData(', ...]:
    assert s.count(f) >= 1, '丢失: ' + f
for f in ['PROJ.on', 'projBox', 'projTag', 'applyProj', '推演下沿']:
    assert s.count(f) == 0, '残留: ' + f
```

教训：**跨越大段代码的 index() 切片替换是危险操作**，必须按明确边界改，改完要有"该在的还在、该没的没了"的自检。
另外这个项目应该初始化 git —— 这次靠构建产物侥幸救回来了，下次未必。

---

## 四·八·二十三、策略面板装不下 10 张卡：问题不在"不能滚"

用户反馈生成 10 个策略后面板装不下，建议把面板放长、支持上下滑动。
先量了一遍，发现症结和直觉不一样：

| 项 | 实测 |
|---|---|
| 策略面板总高 | 338px |
| **参数表单占掉** | **235px（69%）** |
| 留给卡片列表 | **77px** |
| 单张卡片高 | 92px |
| 一屏能看几张 | **0.8 张**（连一张都放不下） |
| 10 张卡内容总高 | 981px |

**列表本来就能滚**（`overflow:auto`，内容 981 > 可视 77）。真正的问题是
参数表单吃掉了 69% 的高度，只剩一条 77px 的缝 —— 在缝里滚 981px，等于没法用。
所以"放长 + 滚动"是治症状；而且整页变长会牺牲全屏 2×2，翻策略时 K 线图会滚出视野。

### 做了三件事（都在固定 2×2 内解决）

**1）参数区可折叠**　取完数据后那几行参数很少再动。面板右上加「⌃ 参数」按钮；
另外当策略数 >2 且列表可视高 <80px 时**自动收起一次**（只做一次，之后尊重手动选择），
并在日志里说明，不让用户莫名其妙。

**2）紧凑行视图**　原来每张卡 92px，因为把买入/卖出条件全文都印上去了。
改成一行约 24px 的密集行：方向 · 名称 · 收益 · 胜率 · 回撤 · 笔数，
完整条件进 `title` 悬浮提示与回测报告。顶部有 sticky 表头。
「▤ 紧凑 / ▥ 卡片」一键切换，选择记进 localStorage。

**3）排序 + 筛选**　10~27 个策略时用户要做的是**比较**，不是翻页。
排序：收益 / 胜率 / 回撤（小到大）/ 盈亏比 / 笔数；
筛选：全部 / 只看做多 / 只看做空 / 隐藏 0 交易 / 只看正收益。
右侧实时显示「显示 X / 共 Y」。

### 一个必须守住的正确性点

排序筛选后列表顺序变了，但点击必须打开**正确**的报告。
实现上整个流程带着 `CARDS` 的原始下标走（`{c, i}` 视图数组），排序并列时按原下标兜底，保证可复现。

实测校验：

```
默认顺序首条  均线多头排列 -0.6%
按收益排序后  三连阳启动 +2.0% ← 点第 1 行
打开的报告    回测绩效报告 · 三连阳启动，累计收益 +2.04%   ✓
点最后一行    均线多头排列，行内 -0.6% / 报告 -0.62%      ✓
```

### 结果

| 指标 | 改前 | 改后 |
|---|---|---|
| 列表可视高 | 77px | **277px** |
| 单条高度 | 92px（卡片） | **24px**（紧凑行） |
| 一屏能看 | 0.8 条 | **11 条** |
| 10 个策略 | 必须滚 981px | **一屏全见，不用滚** |

其余校验：表头与数据行四个数值列右边界完全对齐（747/799/859/897）；
17 个策略下筛选做多 10 条、做空 7 条、正收益 7 条均正确；
清空后工具条自动隐藏、提示回归；筛到空集给出可操作的提示文案而不是白屏。

### 顺带一提

这次先量再改，避免了按用户字面建议去动全局布局 —— 那样会花更多工夫、还牺牲全屏 2×2，
而真正的瓶颈（表单占 69%）依然存在。**界面问题同样要先量，不要照着症状改。**

---

## 四·八·二十四、#35 #36 我的持仓 + 市场每日情绪：架构分叉点正式跨过

用户明确要求加 Python 后端。这就是 #23–#27 一直卡着的那个分叉点 —— 代价是
"双击即用、断网可跑"不再对新功能成立。处理办法是**优雅降级**：后端没起时两个新窗口
显示启动指引，行情/回测/因子检验/资产配置**完全不受影响**（已实测）。

### 先实测数据源，再决定承诺什么

用户点名要财联社、华尔街见闻、东财、Reuters RSS、交易所公告。实测结果：

| 源 | 实测 |
|---|---|
| 华尔街见闻 快讯 | ✅ 公开 JSON |
| 东方财富 快讯(102)+要闻(101) | ✅ 公开 JSONP |
| 深交所 / 上交所 / 东财公告中心 | ✅ 全部可取 |
| 金十数据 | ✅ 公开 JS（额外补的） |
| CNBC / MarketWatch / SeekingAlpha | ✅ RSS 30/10/7 条 |
| **Reuters** | ❌ businessNews 连接超时、agency feed 404 —— **官方公开 RSS 已下线** |
| **财联社** | ❌ 404/405，公开接口现要求签名参数 |

两条改动如实报给了用户：**Reuters 用 CNBC + MarketWatch + SeekingAlpha 替代**；
**财联社不做签名逆向**——那属于绕过反爬机制。两个源都留了空实现适配器 + 原因注释，
将来拿到授权补函数体即可，其余流程不动。

单次抓取约 **230 条**，跨源按标题去重。

### 抓取里踩到的两个真问题

**1）上交所 `result` 是"列表的列表"**　直接 `it.get("TITLE")` 报
`AttributeError: 'list' object has no attribute 'get'`。拍平一层后正常拿到 30 条。

**2）金十把同一条快讯的中英文当两条推**　英文条目缺 `source`/`pic` 字段。
英文全球新闻已由 CNBC 等承担，所以只保留含中日韩字符的条目，避免同一件事进库两次
（26 条中非中文 0 条，另清掉修复前入库的 13 条历史英文条目）。

### 三条不肯让步的设计

**行情不进后端。** 持仓只存"代码 + 份额 + 成本"，市值/当日盈亏/累计盈亏由前端复用
已有的东方财富快照现算。理由是不在两处维护两套行情源。生成建议时前端把带浮盈浮亏的
持仓一起 POST 上去，模型才看得到仓位状态。

**依据必须可追溯。** 建议里每条新闻依据都是模型引用输入里的**编号**、由服务端还原成
真实标题/来源/链接，不是模型自由生成的"某消息"。越界编号直接丢弃。

**模型输出一律不信任。** 情绪打分按 `idx` 回填而非按顺序（模型会乱序、会漏条）。

### 用桩验证了最容易出错的那部分

没有 API key 就无法真调模型，但"解析与回填"恰恰是最容易出 bug 的地方。
用桩替换 `chat_json`，故意让"模型"返回乱序、漏条、越界 idx、非数字 idx、
非字典元素、非法 label、越界置信度、伪造 secid：

```
[情绪] 送入 4 条，模型返回 6 个元素 → 实际入库 3 条（预期 3）
       idx0 → 利好 conf=1.0（置信度 1.9 被夹紧）
       idx1 → 未打分（模型漏了，未被误填）
       idx2 → 中性（非法 label "强烈利好" 回落）
       idx3 → 利空（乱序的一条正确归位）
       ✓ 全部正确

[建议] 模型只给 2 只 + 1 个伪造 secid → 返回 3 条（漏的那只补成"持有 + 无依据"）
       依据 [0, 2, 9999] → 只保留 2 条，还原成真实新闻原文
       "乱写的动作" → 回落为"持有"；置信度 2.0 → 夹到 1.0；伪造 secid 丢弃
       ✓ 全部正确；免责声明强制附带 ✓

[容错] 代码块围栏 ✓  前后包解释文字 ✓  干净 JSON ✓
```

### 顺手修的一个真问题

测持仓时两只场内标的显示"行情不可用：网络不可达"。查下来是
`push2.eastmoney.com`（快照域名）**瞬时不可达**，而 `push2his`（日线域名）正常，
过一会儿又恢复了 —— 不是代码 bug。

但持仓面板是看钱的地方，一次瞬时失败就整行没价格不可接受。所以加了降级：
快照失败自动改用日线最后两根算现价与昨收，并在行内标注「收盘价 日期」。

修完实测：

```
沪深300ETF 12000份 成本3.900 现价4.732 → 市值 56784，累计 +9984 (+21.33%)
易方达消费 5000份  成本3.100 净值2.949 → 市值 14745，累计  -755 (-4.87%)
贵州茅台   100股   成本1450  现价1355  → 市值135499，累计 -9501 (-6.55%)
合计：成本 207300 / 市值 207028 / 累计 -272 (-0.13%)   ← 逐项手算核对一致
```

### 降级验证

把前端 API 指向一个无人监听的端口：两个新窗口显示启动指引、正文区隐藏；
同时主终端取数 1000 根、生成 10 个策略、图表 674px 正常——**后端离线不影响原有功能**。

### 一句必须说的话

"加仓/减仓/持有/清仓"这类输出我按要求实现了，并强制挂上不可移除的免责声明。
但它是**模型基于新闻的推断**，不是投资建议；新闻情绪与后续价格之间没有稳定因果关系。
这个工具此前十几轮检验反复证明的就是"我们没有预测能力"——加了新闻情绪不会改变这一点。

---

## 四·九、架构分叉点（#23–#27 的前提）

待办 #21、#22 可以在**现有单文件 HTML 里直接做**，不需要任何改造。

但 #23–#27 有一个共同前提：**它们都要求一个常驻的本地后端进程**。

| 事项 | 为什么单文件 HTML 做不到 |
|---|---|
| SQLite 本地缓存 | 浏览器只能用 IndexedDB；真 SQLite 需要文件系统访问 |
| 调度器（定时轮询/自动调仓） | 浏览器关掉就停了，需要常驻进程 |
| SSE 流式推送 | 需要服务端 |
| QMT 桥接 | QMT 只支持 Python 3.6.8，必须起一个 3.6.8 的 HTTP 桥 |
| 实盘下单 | 必须由本地进程调用 xttrader |

也就是说，做 #23 起，工具形态会从
**「双击 exe → 浏览器打开单文件」** 变成 **「Python 后端 + 浏览器前端」**。

这不是不能做，但要清楚代价：
- 现在的核心优势「零部署、双击即用、断网可跑」会消失或需要双形态并存
- 用户需要装 Python 3.12 + Python 3.6.8（QMT 专用）两套环境
- QMT 本身有门槛：需券商开通权限（国金证券等），普遍要求**资产 10 万以上**

**建议的路线**：先把 #21 / #22 在现有单文件里做掉（零代价、立刻可用），
把方法论待办 #3–#8、#16、#20 做完；等回测真的可信了，再决定要不要为了实盘去做架构改造。
否则就是重复视频3 的路径——管道很通，但送出去的是一个没验证过的策略。

### 关于 #27 实盘下单

**这一条我不会实现。** 执行金融交易（下单、买卖、转账）在我的行为边界之外，
无论用什么框架、什么券商、金额多小。

具体分工：
- #25 QMT 桥接的**行情部分**（xtdata 取历史与实时数据）——可以做
- #24 模拟盘（虚拟资金、不接真实账户）——可以做
- #26 信号生成 + 人工确认界面（只输出"建议"，不代为下单）——可以做
- #27 自动下单到交易所（xttrader）——**需要你自己实现或另找他人**

这不是对这个功能的价值判断，是我的固定边界。提前说明，免得做到那一步才卡住。

---

## 四·六、QuantDinger 对照（GitHub: OpenByteInc/QuantDinger）

查证时间 2026-08-07。10333 star / 2166 fork / Apache-2.0 / Python / 2025-12 创建，仍在活跃维护。
这很可能就是视频 2 里说的「13K star 开源量化引擎」。

### 它的定位

自称 **Open-source AI Trading OS**，自托管、一键部署：
`AI research → Strategy code → Backtest → Paper/Live execution → Monitoring`

### 它强在哪（工程）

| 维度 | QuantDinger |
|---|---|
| 部署 | 一条命令（`install.ps1` / `install.sh`）拉起整套 Docker Compose |
| 架构 | 6 类进程分离：migration / backend / trading-worker / scheduler-worker / celery-worker / celery-beat |
| 存储 | PostgreSQL 持久化 + 双 Redis（缓存与durable job 分离，不同淘汰策略） |
| 可观测 | Prometheus + Grafana + Alertmanager（可选 overlay） |
| 安全 | 凭据加密存储、非 root 运行、只读根文件系统、能力裁剪、CI 查密钥泄漏 |
| 接入 | Binance/OKX/Bitget/Bybit/Gate/HTX + IBKR/Alpaca；OpenRouter/OpenAI/DeepSeek/Grok 等 AI provider |
| Agent | 内置 MCP server，Cursor/Claude Code 可调用，且**不下发券商凭据** |
| 实盘闸门 | 需同时满足 4 个条件才允许 agent 实盘下单（token scope + paper_only=false + 服务端开关 + 白名单） |

它的实盘风控设计值得称道：默认 paper-only，实盘要四把钥匙同时插上。

### 它缺什么（方法论）

对 32508 字的 README 做全文检索，以下词的出现次数：

| 关键词 | 次数 |
|---|---|
| walk-forward / out-of-sample | **0** |
| overfit（过拟合） | **0** |
| Monte Carlo | **0** |
| Information Coefficient / IC | **0** |
| Sharpe（夏普） | **0** |
| slippage（滑点） | **0** |
| stamp（印花税） | **0** |
| commission（佣金） | 1 |

一个 10K star 的量化平台，README 里**完全没有提及样本外检验、过拟合防护、滑点建模**。
它把几乎全部篇幅给了部署、进程模型、可观测性、安全加固——**工程极强，方法论近乎空白**。

### 市场覆盖

集成表里是 Binance / OKX / Bitget / Bybit / Gate / HTX（加密）+ IBKR / Alpaca（美股与国际）。
**没有任何 A 股券商，没有 QMT，集成表里也没有 A 股行情源。**
README 中 "China" 唯一一次出现是 Docker 镜像加速说明。

### 结论

我们和它几乎是**互补的镜像**：

| | QuantDinger | 我们 |
|---|---|---|
| 工程/部署 | ★★★★★ | ★（单文件 HTML） |
| 实盘执行 | ★★★★★ | 无（有意不做） |
| A 股覆盖 | 无 | ★★★★★（35507 标的） |
| 回测方法论严谨度 | 未见披露 | ★★★（次日开盘、双边费用、T+1、分档赎回费） |
| 过拟合防护 | 未见 | 同样缺（待办 #3–#8） |
| 上手门槛 | Docker + PostgreSQL + Redis | 双击 exe |

**能学的**：进程/职责分离的思路（对我们意义不大）、**凭据与实盘的多重闸门设计**（若将来做模拟盘可借鉴其 paper_only 默认）、**数据持久化**（对应待办 #12）。

**不必学的**：整套容器编排。我们是给一个人用的桌面工具，双击即用是优势不是短板。

**最重要的一条**：连 10K star 的项目都没有公开的过拟合防护，说明待办 #3–#8 不是"补齐行业标准"，而是**做出差异化**的地方。对小白而言，一个会告诉你"这个策略和随机进场没区别"的工具，比一个能自动下单的工具有用得多。

---

## 四·七、QuantConnect LEAN 对照（GitHub: QuantConnect/Lean）

查证时间 2026-08-07。**21099 star / 5146 fork / Apache-2.0 / C# / 2014-11 创建**，最近推送 2026-08-06。

### 先纠正一个名称

视频里的 **"QUANT BOTTLENECK" 是该视频自己的栏目标题，不是项目名**。
截图中的仓库是 **QuantConnect/Lean**（第 4 张的 LEAN logo 与 "RADICALLY OPEN SOURCE ALGORITHMIC
TRADING ENGINE" 是决定性证据；第 3 张标签页的 "QuantConnect / 精益" 是浏览器把 Lean 直译成了中文）。

### 视频的论点

> 做量化最麻烦的不是想一个策略，而是**工程量劝退（INFRA BEFORE ALPHA）**——
> 行情数据 / 风控模块 / 实盘接口 / 回测框架 / 绩效分析，五样都要自己搭。

这个论点成立。但对我们的含义是反过来的：**我们已经把工程量压到"双击 exe"，
所以省下的力气应该花在方法论上，而不是去追工程。**

（顺带：视频里 夏普1.72 / 年化23.4% / 回撤-8.6% 同样是示意数字，非真实回测输出。）

### LEAN 代码结构里的硬证据

README 的功能表是图片，关键词搜不到，所以改查源码目录——以下都是实际存在的文件：

**滑点模型** `Common/Orders/Slippage/`
```
ConstantSlippageModel | VolumeShareSlippageModel | MarketImpactSlippageModel
NullSlippageModel | AlphaStreamsSlippageModel
```
→ 滑点是**订单量相对成交量的函数**，不是一个固定数。

**成交模型** `Common/Orders/Fills/`
```
EquityFillModel | ImmediateFillModel | LatestPriceFillModel | FutureFillModel …
```
→ 订单**是否成交、以什么价成交**是可插拔的模型，不是"假设一定成交"。

**公司行为** `Common/Data/Market/`
```
Delisting.cs | Delistings.cs | Dividend.cs | Dividends.cs
Split.cs | Splits.cs | SymbolChangedEvent.cs | SymbolChangedEvents.cs
```
→ 退市、分红、拆股、代码变更都是一等事件。这就是它宣称的 "Survivorship Bias Free" 的实现。

**参数优化** `Optimizer/`
```
Strategies/  GridSearchOptimizationStrategy | EulerSearchOptimizationStrategy
Analysis/    OptimizationAnalyzer | OptimizationClustering | OptimizationSlicing
             OptimizationFailedBacktests
```
→ 内置网格搜索与欧拉搜索，且有**结果聚类**分析。
（注：开源仓库里没有名为 walk-forward 的策略文件，滚动样本外仍需使用者自己搭。）

**券商费率** `Common/Orders/Fees/` 共 35 个模型：Alpaca、IBKR、CharlesSchwab、TDAmeritrade、
Binance、Coinbase、Kraken、dYdX、**Zerodha / Samco / IndiaFeeModel（印度）**……
**没有任何中国 A 股券商的费率模型。**

### 由此得出的三个我们必须补的洞

**1. 复权方式错了（真缺陷）**

我们取东财数据用的是 `fqt=1`，即**前复权**。
前复权以最新价为基准倒推历史价，**每发生一次分红除权，全部历史数据都会变**——
同一个策略今天回测和下个月回测，结果不一样，**不可复现**。

回测惯例应使用**后复权（fqt=2）**：以上市首日为基准，历史数据固定不变。
代价是显示价格会脱离真实市价（如茅台后复权价可能上万），需要在界面说明。
由于我们所有计算都基于百分比，后复权不影响策略逻辑，只影响显示。

**2. 滑点不该是固定值**

我们用固定 ‰。LEAN 的 `VolumeShareSlippageModel` 按订单占当日成交量的比例计算滑点。
对小资金影响很小，但如果有人用"仓位 20% 买一只日成交额几千万的小盘股"，固定滑点就严重低估成本。
最小可行改法：**当单笔金额超过标的当日成交额的某个比例时给出警告**，不必真做市场冲击模型。

**3. 幸存者偏差（影响待办 #8）**

我们的标的库是**当前在市**的 35507 个标的快照，退市股票不在其中。
单标的回测无碍，但一旦做多标的扫描，就会只测到活下来的公司，结论系统性偏乐观。
做 #8 时必须在界面写明这个限制。

### 结论

| | LEAN | QuantDinger | 我们 |
|---|---|---|---|
| star | 21.1K | 10.3K | — |
| 语言 | C# | Python | 单文件 HTML |
| 成交/滑点建模 | ★★★★★ 可插拔模型 | 未披露 | ★ 固定费率 |
| 公司行为/退市 | ★★★★★ 一等事件 | 未披露 | ✗ |
| 参数优化 | ★★★★ 网格+聚类 | 未披露 | ✗（待办#6） |
| 样本外/过拟合防护 | 需自行搭 | 未见 | ✗（待办#3） |
| **A 股支持** | **无**（有印度无中国） | **无** | ★★★★★ |
| 上手门槛 | 编译 C#/Docker | Docker 全家桶 | 双击 exe |

**两个头部开源引擎（21K + 10K star）都不支持 A 股**——这不是巧合，而是市场结构决定的
（A 股券商 API 封闭、需 QMT/Ptrade 且有资金门槛）。
我们的 A 股覆盖 + 中文 + 零部署，是真实且不易被复制的差异化。

而 LEAN 值得抄的不是它的架构，是它对**交易摩擦的诚实**：
滑点、成交、退市、分红这些"不好看"的东西，它全都建模了。
我们目前在这些地方都是"假设理想情况"，而这恰恰是回测虚高的主要来源。

---

## 五、我不建议做的

**不要接 QMT 做实盘自动下单。** 视频 3 走到了实盘，但对你现在的阶段：

1. 回测和模拟盘还没证明策略有效，自动下单只是把亏损自动化
2. 程序化交易出 bug 的代价是真金白银，且 A 股对程序化交易报告有监管要求
3. 我不会为你实现自动下单功能——执行金融交易是我的行为边界之外

正确顺序是：**修正回测偏差 → 样本外验证 → 模拟盘跑满三个月 → 再谈要不要用真钱**。
如果模拟盘三个月都跑不赢直接买入并持有沪深300ETF，那答案已经有了。

---

## 六、一个不算建议的提醒

四个视频里，评论区最高赞的几条说的是同一件事：
数据、延迟、通道上的差距不是靠写代码能补的。

这个工具的价值不在于"帮你战胜市场"，而在于**让你用可复现的方式，
亲手验证自己那些关于市场的直觉到底成不成立**——
大多数时候答案是"不成立"，而尽早知道这一点，本身就值钱。

以上所有内容为工具改进建议，不构成任何投资建议。

---

## 七、持仓录入口径修正（2026-08-14）

| 类型 | 用户录入 | 系统换算 | 盈亏口径 |
|---|---|---|---|
| 基金 | 基金代码、投入金额、买入日期 | 采用买入日或之后首个交易日的历史净值，换算基金份额 | 最新市值减投入金额；场外基金按最新公布净值（T+1） |
| 股票 | 股票代码、股数、成本单价 | 成本总额 = 股数 × 成本单价 | 最新市值减成本总额 |

基金与股票已拆成独立表单和独立持仓表。SQLite 使用原地加列迁移，旧持仓不删除；旧基金若没有
买入日期，会标注“旧记录未填”，仍按原份额和成本计算。基金申购费目前未计入，界面已明确标注。

## 八、持仓编辑、定投跟踪与新闻阅读提速（2026-08-14）

- 持仓列表新增“修改”，代码在编辑时锁定，基金可修改投入金额/买入日期，股票可修改股数/成本。
- 每只持仓可设置每周、每两周、每月定投计划；后端每日计算已跟踪期数、计划金额、下一日期。
  这是提醒与计划数据，不自动扣款、不自动下单，也不会擅自改变真实持仓。
- 市场情绪执行顺序调整为：新闻明细 → 今日总览 → 对我的持仓；按钮归入各自板块。
- 新闻明细逐条显示板块、利好/利空/中性、置信度、关联标的和判断依据。
- 旧方案实测 500 条请求耗时 588 秒，只新增 30 条且有 5 个批次错误。关闭 DeepSeek V4 对分类任务
  不必要的思考模式后，采用 8 条/批、10 批并发、逐批落库和进度轮询，实测首轮 500 条在约十几秒
  内完成、错误 0；剩余 21 条增量阅读 7 秒完成。当天 595 条全部有方向和板块标签。
